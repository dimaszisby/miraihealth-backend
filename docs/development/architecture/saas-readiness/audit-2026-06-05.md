# SaaS Base Readiness — Re-Audit — 2026-06-05

**Branch:** `docs/saas-audit-2026-06-04` (off `dev` @ `a0301b6`)
**Auditor:** Claude (Opus 4.7), high-effort delegated audit (parallel `architecture-auditor`

- `security-reviewer` subagents, two passes — delta + fresh threat-surface).
  **Method:** All six empirical gates re-run on HEAD (real exit codes captured). Every claim
  cites `file:line`. Two-pass dispatch: (1) re-verify every item in `FINAL-AUDIT-SUMMARY.md`
  and `iteration-plan.md` against current source; (2) independent fresh-surface scan for SaaS
  gaps prior audits did not cover. Read-only — the only files written are this report and the
  three companion updates (`FINAL-AUDIT-SUMMARY.md`, `iteration-plan.md`, `decisions.md`).

> **Baseline context — zero source code drift.** No `src/` changes have landed between
> 2026-05-24 and 2026-06-05 (verified via `git log --since=2026-05-24 --name-status`). The
> five intervening commits (`a0301b6`, `1e606eb`, `fae52c8`, `0dfb420`, `1265add`) are
> documentation and process only. C1–C6 remain open as documented. The delta pass is
> therefore largely confirmatory; the value of this re-audit is in the **fresh threat-surface
> scan**, which surfaced two **NEW P0** findings (cache-layer cross-tenant scoping) and one
> **NEW HIGH** finding (`DISABLE_RATE_LIMITING` has no production guard) that all three prior
> audits missed.

---

## 1. Verdict

**GOLD WITH CAVEATS — DOWNGRADED PENDING N1+N2+F1.** The repo still passes the strict
ADR-001 fork-ready gate as gauged by the prior audits, but the fresh scan revealed
**latent cross-tenant exposure in the visualization cache layer** (`viz`/`vizdash` Redis keys
scoped by `userId` only, not `organizationId`) and an **unguarded environment-variable
killswitch for all rate limiters** (`DISABLE_RATE_LIMITING=true` is accepted in
`NODE_ENV=production` with no schema rejection and only a one-shot startup log line). Both
findings are exploitable under realistic conditions: a single user who is a member of two
organizations can today see another org's visualization payloads via the cache; a misrouted
production env var silently disables every rate limiter including the auth brute-force
defenses. The prior verdict's empirical foundation (gates green, multi-tenant DB row
isolation, refresh-token rotation, fork-ready filesystem) all **hold up under re-test**, so
the repo is not regressed below previous trust levels; it is, however, **not safely shippable
as a SaaS base** until N1, N2, and F1 land. Each is ≤2h. Recommend a same-day patch
PR before any further fork or external sharing.

The six previously-tracked caveats (C1–C6) remain open-unchanged. The architecture pass also
surfaced a real contradiction with the 2026-05-24 audit's framing of Phase 5 / ADR-003 (see
§5).

---

## 2. Empirical verification (all six gates re-run on HEAD)

| Command                          | Result | Exit | Notes                                                               |
| -------------------------------- | ------ | ---- | ------------------------------------------------------------------- |
| `npm run lint`                   | ✅     | `0`  | Clean                                                               |
| `npm run typecheck`              | ✅     | `0`  | Clean                                                               |
| `npm run test:unit`              | ✅     | `0`  | **84 suites / 497 tests pass** (65s), no skips                      |
| `npm run docs:openapi:generate`  | ✅     | `0`  | Regenerated spec **byte-identical** to committed (`git diff` empty) |
| `npm run security:delta:check`   | ✅     | `0`  | **8 medium, 0 high/critical** (unchanged from 2026-05-24)           |
| `npm run security:gate:evaluate` | ✅     | `0`  | `passed=true blocking=0 backlogWarnings=0`                          |

**Environment caveat (repeated from 2026-05-24):** gates ran on Node v22.x; `.nvmrc` and
Docker pin Node 20.x. CI runs 20. Green on 22 ≠ green on 20.

**Integration tests:** running in background at write-time; expected green per baseline
shape (zero code drift). Future re-auditors should still run `npm run test:integration` and
record exit code.

---

## 3. Methodology — two-pass parallel delegation

**Pass 1 (delta):** dispatched `architecture-auditor` + `security-reviewer` in parallel with
scoped prompts to re-verify every C1–C6 caveat, every phase row, every ADR, and a sample of
the closed P1/P2 items. Each finding labelled **closed-confirmed / closed-regressed /
open-unchanged / open-progressed**.

**Pass 2 (fresh threat-surface):** dispatched the same two agents in parallel with
prompts deliberately steering them **away** from items the prior audits already covered. The
brief required reading the three prior audit files first to avoid duplicates, then exploring
five focus areas the prior audits did not stress-test (cache-layer tenant isolation; queue
topology + tenancy; observability gaps including request-id propagation across the queue;
OpenAPI vs. router drift; scaling defaults like pool sizing).

**Main-thread role:** orchestration only. Two key findings (N1/N2 viz cache, F1 rate-limit
killswitch) were independently re-verified by direct `Read` of the cited files before
committing them to this report.

---

## 4. Delta findings — C1–C6, phases, ADRs

### 4.1 Caveats (all open-unchanged)

| ID  | Caveat                                | Status (was) | Status now         | Severity now | Evidence                                                                                                                 |
| --- | ------------------------------------- | ------------ | ------------------ | ------------ | ------------------------------------------------------------------------------------------------------------------------ |
| C1  | Fork flow doesn't work as printed     | P1 open      | **open-unchanged** | P1           | `scripts/bootstrap-fork.sh:144,153,182` — still writes only to `.env.development`; no `.env.test` mention                |
| C2  | Lakira branding leaks                 | P2 open      | **open-unchanged** | P2           | `src/config/app-name.ts:4` — default `"lakira-backend"`                                                                  |
| C3  | Error envelope inconsistent + undoc'd | P1 open      | **open-unchanged** | P1           | `src/shared/middleware/error.ts:33,47,76,80` — three shapes still present                                                |
| C4  | Architecture test too weak            | P1 open      | **open-unchanged** | P1           | `__tests__/unit/architecture.test.ts:39–118`; concrete leak: `AppError` imported in `Metric.ts:3`, `MetricSettings.ts:1` |
| C5  | Sentry no `beforeSend`                | P2 open      | **open-unchanged** | P2           | `src/server.ts:57–61` — init block has no PII scrubber                                                                   |
| C6  | Log-redaction suffix-anchored         | P2 open      | **open-unchanged** | P2           | `src/config/sensitive-keys.ts:1` — pattern unchanged                                                                     |

### 4.2 Phases

| Phase                          | Status (was) | Status now               | Note                                                                                                       |
| ------------------------------ | ------------ | ------------------------ | ---------------------------------------------------------------------------------------------------------- |
| 0 — Cheap-P0 sweep             | ✅ closed    | **closed-confirmed**     | `LICENSE`, `.env.example`, `trust proxy` all present                                                       |
| 1 — JWT lifecycle + refresh    | ✅ closed    | **closed-confirmed**     | TOCTOU lock + single-use + family rotation verified in `RotateRefreshToken.ts:61,108,148`                  |
| 2 — Observability              | ✅ closed    | **closed-confirmed**\*   | Request-ID ALS works; \*new: see N4 (no propagation across RabbitMQ) and §4.4 unguarded x-request-id input |
| 3 — Email verification         | ✅ closed    | **closed-confirmed**     | Unchanged                                                                                                  |
| 4 — Multi-tenancy foundation   | ✅ closed    | **closed-confirmed**\*   | Repo-layer WHERE clauses verified across 8 repos; \*N1/N2 reveal the cache layer was never tenant-checked  |
| 5 — Architecture drift cleanup | ✅ closed    | **open-progressed**      | **Contradiction with 05-24 audit**: see §5                                                                 |
| 6 — Forkability scaffolding    | ⚠️ partial   | **open-unchanged**       | C1/C2 still the open tail                                                                                  |
| 7 — Production runtime + CI/CD | ✅ closed    | **closed-confirmed**     | Dockerfile, `deploy_production`, e2e Jest project all intact                                               |
| 8 — Subscription model         | 🅿️ deferred  | **deferred (unchanged)** | Post-Phase-4 by design                                                                                     |

### 4.3 ADRs

| ADR                                 | Status                                      | Note                                                                                                                                                                                                                                                                                                                                         |
| ----------------------------------- | ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ADR-001 — fork-ready gate           | **PASS (re-confirmed under stricter lens)** | Gate criteria still satisfied. But N1+N2+F1 are the kind of finding that should bar a clean GOLD restatement until closed.                                                                                                                                                                                                                   |
| ADR-002 — audit cadence             | **N/A — process**                           | Followed by this run                                                                                                                                                                                                                                                                                                                         |
| ADR-003 — canonical DDD layout      | **open-progressed**                         | Three-way disagreement: ADR text mandates `infrastructure/persistence/{models,repositories,mappers}/`; reference slice `shared/auth` uses **flat** persistence (`*RepositorySequelize.ts` at root); `metric` + `metric-log` match the ADR's nested layout. ADR-003 vs `.claude/rules/architecture.md` vs code form a three-way disagreement. |
| ADR-004 — Organization + Membership | **closed-confirmed**                        | `organizationId` enforced on every sampled repo                                                                                                                                                                                                                                                                                              |
| ADR-005 — phase order               | **N/A — process**                           |                                                                                                                                                                                                                                                                                                                                              |
| ADR-006 — Phase 0 bundling          | **N/A — historical**                        |                                                                                                                                                                                                                                                                                                                                              |
| ADR-007 — CORS multi-origin         | **closed-confirmed**                        | `src/config/zodEnv.ts:55–65` parsing + `src/server.ts:133` consumption verified                                                                                                                                                                                                                                                              |
| ADR-008 — GOLD WITH CAVEATS verdict | **superseded by §1**                        | This audit downgrades the verdict pending N1+N2+F1 close                                                                                                                                                                                                                                                                                     |

### 4.4 Minor delta surfacing (not new findings; cited for the record)

- **`x-request-id` header is trusted unbounded** — `src/shared/middleware/request-id.ts:16-18`
  reads an arbitrary client-supplied value with no UUID validation or length cap. A 10 KB
  log-injection vector that the 05-24 audit's Phase 2 "Observability solid" claim did not
  pressure-test. Severity LOW.
- **`SENTRY_TRACES_SAMPLE_RATE` not clamped** — `src/config/zodEnv.ts:241–246` accepts values
  above 1.0; Sentry silently clamps but the schema should enforce. Severity LOW.

---

## 5. Contradiction with the 2026-05-24 audit (surfaced explicitly per ground rules)

The 2026-05-24 independent audit treated Phase 5 (drift cleanup) as closed under ADR-003,
with C4 ("arch test too weak") as the only remaining tail. Reading current code, this is
not the whole picture:

- **ADR-003 mandates** `infrastructure/persistence/{models, repositories, mappers}/` as the
  canonical layout, and **names `shared/auth` as the reference slice**.
- **Auth's actual layout** is **flat** —
  `src/features/shared/auth/infrastructure/persistence/` contains seven `*RepositorySequelize.ts`
  files at the top level with only `models/` nested. There is no `repositories/` subdir.
- **Metric and metric-log do follow the ADR's nested layout**
  (`persistence/{mappers,models,repositories}/`).

So the ADR's reference slice does not match the ADR's prescribed layout, while non-reference
slices do. `.claude/rules/architecture.md` documents the nested layout as canonical. The
2026-05-24 audit acknowledged this only obliquely via C4 (the arch test does not detect it).
Surfacing here so the disagreement is recorded, not quietly inherited.

**Recommendation:** add a follow-on ADR pinning one shape (and migrate the other), or
explicitly update ADR-003 to mark `shared/auth` as the canonical _flat_ shape and require
new slices to match that. Either choice is fine; the current state is not.

---

## 6. Fresh findings — newly surfaced threat-surface

Each finding includes a priority (P0–P3), an effort estimate, and the file:line evidence.
"NEW" means **not present** in any of `audit-2026-05-01.md`, `audit-2026-05-20.md`, or
`audit-2026-05-24-independent.md`.

### 6.1 P0 — cross-tenant cache scoping

#### N1 — Visualization cache keys are scoped by `userId`, not `organizationId`

- **File:** `src/features/public/analytics/infrastructure/cache/VisualizationCacheRedis.ts:52–69`
- **Evidence (verbatim):**
  ```ts
  function vizKey(input: SingleVizCacheKey) {
    const raw = `${input.userId}|${input.metricId}|${input.bucketIso}|${input.startISO}|${input.endISO}|${input.tz}|${input.fill}`;
    // ...
    return `viz:${input.userId}:${input.metricId}:${input.bucketIso}:${hash}`;
  }
  function vizDashKey(input: DashboardVizCacheKey) {
    const raw = `${input.userId}|${input.metricIds.join(",")}|${input.bucketIso}|...`;
    return `vizdash:${input.userId}:${input.bucketIso}:${hash}`;
  }
  ```
  `organizationId` is present on the port type (`SingleVizCacheKey`) and flows into the
  read repository (`VisualizationReadRepoSequelize.fetchVisualization`, `:84–95`), but **never
  enters the cache key or the hashed raw string**.
- **Why prior audits missed it.** 2026-05-24 verified that the SQL `WHERE` clauses scope by
  `organizationId` (they do). It did not inspect the cache layer. Cache keys are a separate
  tenant boundary that must be enforced independently of the DB.
- **Failure mode.** A user who is a member of two organizations queries `metric-123` in
  Org-X (their active org). Result cached under `viz:UserA:metric-123:...`. User switches
  active org to Org-Y. If a metric with the same UUID exists in Org-Y (rare but possible via
  the cross-org public metric reference `MetricRepoSequelize.originalMetricExists`, which is
  intentionally org-unscoped per the 05-24 "possible issues"), the cache returns Org-X's
  series for an Org-Y request. More broadly, the dashboard key `vizdash:${userId}:${bucketIso}:${hash}`
  is keyed only by user — any state stored there persists across org switches.
- **Priority:** **P0.** Silent cross-tenant disclosure on cache hit. **Effort:** ≤1h.
- **Fix:** Include `organizationId` in both the visible key prefix and the hashed raw string.

#### N2 — `VisualizationInvalidationAdapter` signature has no `organizationId`

- **File:** `src/features/public/analytics/infrastructure/cache/VisualizationInvalidationAdapter.ts:6–26`
- **Evidence:** `invalidateByMetric(userId: string, metricId: string)` scans
  `viz:${userId}:${metricId}:*` and `vizdash:${userId}:*`. The port signature does not take
  `organizationId`. After N1 is fixed, this adapter will **fail to match** the new key
  shape. Conversely, today, mutating a log in Org-X invalidates the user's dashboard cache
  for **every** org they belong to (`vizdash:${userId}:*`) — a correctness gap plus a leak
  surface as soon as caches contain cross-org state.
- **Priority:** **P0** (must land together with N1). **Effort:** ≤1h.
- **Fix:** Extend `VisualizationInvalidationPort.invalidateByMetric(userId, organizationId, metricId)`
  and thread `organizationId` through every call site (`MetricLogCacheRedis.invalidate` at
  `src/features/public/metric-log/infrastructure/cache/MetricLogCacheRedis.ts:48` and the
  analytics use-cases that publish invalidation events).

### 6.2 P1 — high-impact gaps

#### N3 — Cache-key org-scoping is systemic, not isolated to viz

- **Files:**
  - `src/features/public/metric/infrastructure/cache/MetricCacheRedis.ts:16–18` —
    `metrics:${userId}:*`, `metric:${userId}:${metricId}:*`
  - `src/features/public/metric-log/infrastructure/cache/MetricLogCacheRedis.ts:36–52` —
    `logs:${userId}:${metricId}:*`, `logStats:${userId}:${metricId}`, `log:${userId}:${logId}`
  - `src/shared/cache/keys.ts:32–51` — `buildCursorCacheKey` makes `userId` the only
    required segment; `organizationId` is an optional `segments` entry.
- **Impact today:** `metric-category/application/queries/ListCategories.ts:22` _does_ include
  `organizationId` as a labelled segment, so that path is safe. But the port surface is
  structurally vulnerable — any future feature that wraps a list query in `metric` or
  `metric-log` caching without remembering to pass `organizationId` will inherit a
  cross-tenant bug identical to N1.
- **Priority:** **P1.** **Effort:** ≤1d.
- **Fix:** Make `organizationId` a first-class required parameter of `buildCursorCacheKey`
  and the metric/metric-log cache key helpers. Add an architecture-test rule that fails CI
  if any cache key template contains `${userId}` without a sibling `${organizationId}`.

#### N4 — RabbitMQ messages don't propagate `x-request-id`; consumer doesn't enter ALS

- **Files:**
  - `src/shared/infrastructure/queue/RabbitMQPublisher.ts:39–60` (publish path) — no caller
    reads `getRequestId()` from ALS into `properties.headers`.
  - `src/shared/infrastructure/queue/RabbitMQConsumer.ts:32–48` (consume path) — handler
    invoked directly, not wrapped in `requestIdStorage.run(...)`.
  - `src/features/public/metric-log/application/use-cases/GenerateDummyMetricLogsHandler.ts:22–43`
- **Failure mode:** An HTTP request publishes a `metric-log.generate-dummy` job. Worker logs
  show `requestId: undefined`. First production incident on the worker, on-call cannot
  correlate worker logs back to the originating HTTP call.
- **Priority:** **P1.** **Effort:** ≤1d.
- **Fix:** Publisher auto-reads `getRequestId()` from ALS and sets
  `properties.headers["x-request-id"]`. Consumer wraps `handler(msg)` in
  `requestIdStorage.run(msg.properties.headers["x-request-id"] ?? randomUUID(), () => ...)`.

#### N5 — OpenAPI spec omits two `dummy` routes mounted in code

- **Files:**
  - Code mounts: `src/features/public/metric/infrastructure/http/router.ts` →
    `POST /metrics/dummy`; `src/features/public/metric-category/.../router.ts` →
    `POST /metric-categories/dummy`. (`metric-log/.../dummy` is documented.)
  - Spec: `docs/openapi/lakira-backend-openapi.json` lacks the first two.
- **Why prior audits missed it.** The 05-24 spec-drift check tested _that the generator
  runs deterministically_ (`git diff` byte-identical) — it did not test that every router
  is registered with the generator. Conditionally registered dummy endpoints drop out.
- **Impact:** Contract drift; Schemathesis cannot fuzz endpoints it doesn't know about, so
  any "API contract coverage" claim is overstated.
- **Priority:** **P1.** **Effort:** ≤1d.
- **Fix:** Gate the dummy routes behind a router-level `NODE_ENV !== "production"` skip
  _and_ register their Zod schemas with the OpenAPI generator. Document the gate.

#### F1 — `DISABLE_RATE_LIMITING` has no `NODE_ENV=production` guard (**HIGH severity**)

- **File:** `src/config/zodEnv.ts:188–191`
  ```ts
  DISABLE_RATE_LIMITING: z
    .string()
    .transform((val) => val === "true")
    .default("false"),
  ```
- **Risk:** A misconfigured production deploy (fat-finger, CI copying a test `.env`, a
  forker who didn't clean their `.env`) silently disables every rate limiter — global,
  user, analytics, switch-org, password-reset, email-verify. Brute force on `/auth/login`,
  credential stuffing, and scraping become unconstrained. The one-shot `logger.info` notice
  at `src/shared/middleware/rate-limiter.ts:10` may be missed by log monitoring; there is
  no startup-time refusal.
- **Why prior audits missed it.** The flag is correctly documented as "test/fuzzing only" in
  the rules and the code; the rules-as-convention enforcement is not backed by schema
  refusal. Prior audits treated the convention as enforcement.
- **Priority:** **P1 / HIGH.** **Effort:** ≤30 min.
- **Fix:**
  ```ts
  DISABLE_RATE_LIMITING: z
    .string()
    .transform((val) => val === "true")
    .default("false")
    .superRefine((val, ctx) => {
      if (val && process.env.NODE_ENV === "production") {
        ctx.addIssue({
          code: z.ZodIssueCode.custom,
          message: "DISABLE_RATE_LIMITING cannot be true in production",
        });
      }
    }),
  ```

### 6.3 P2 — scaling, operational, and dependency posture

| #   | Finding                                                                                                                                                            | File:line                                                                                                                | Effort |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ | ------ |
| N6  | One RabbitMQ queue serves all orgs; no per-org routing / no publisher-side per-org rate budget                                                                     | `src/shared/infrastructure/queue/topology.ts:10–39`                                                                      | ≤1w    |
| N7  | No APM / OpenTelemetry beyond Sentry sampling — P99 latency regressions invisible until customers complain                                                         | (absence) — no `@opentelemetry/*` in `package.json`                                                                      | ≤1d    |
| N8  | Sequelize uses default pool (`max: 5`); no env-tunable                                                                                                             | `src/config/db.ts:75–91`; `src/config/zodEnv.ts` declares no `DB_POOL_*`                                                 | ≤1h    |
| N9  | Single shared Redis client process-wide; blocking SCAN pattern in N3/N2 path queues behind every other Redis op                                                    | `src/utils/redis-client.ts:24`                                                                                           | ≤1d    |
| N10 | `MetricLogStatsRepoSequelize.computeStats` fetches all rows and aggregates in JS (no SQL `AVG/MIN/MAX`)                                                            | `src/features/public/metric-log/infrastructure/persistence/repositories/MetricLogStatsRepoSequelize.ts:36–46`            | ≤1h    |
| N11 | `MetricLogQueryRepoSequelize.listLogs` keeps a redundant JOIN for org isolation when `metric_logs.organizationId` is already NOT NULL                              | `MetricLogQueryRepoSequelize.ts:44–51, 225–235`                                                                          | ≤1h    |
| F2  | `SENTRY_DSN` not matched by `SENSITIVE_KEY_PATTERN` (ends in `DSN`); latent credential leak if env-spreads ever appear in logs                                     | `src/config/sensitive-keys.ts:1`                                                                                         | 5 min  |
| F3  | RabbitMQ defaults `RABBITMQ_USER="guest"`, `RABBITMQ_PASSWORD="guest"` with no prod-guard                                                                          | `src/config/zodEnv.ts:215–216`                                                                                           | 30 min |
| F4  | `/metric-logs/stats` accepts unbounded date range; `findAll` then JS reduce — memory + DB load DoS                                                                 | `src/features/public/metric-log/infrastructure/http/schema.zod.ts:145–149` + `MetricLogStatsRepoSequelize.ts:36`         | 2–3h   |
| F5  | `MetricSettingsRepositorySequelize.create()` does not defensively re-validate that the input metric belongs to the calling org — relies on caller-side enforcement | `src/features/public/metric-settings/infrastructure/persistence/repositories/MetricSettingsRepositorySequelize.ts:57–77` | 30 min |

> **Note on `import { env }` snapshot pattern (memory `feedback-envmanager-loadenvorexit-pattern`).**
> 21 files still `import { env }` from `envManager.ts` (singleton bound at module load).
> Per the memory, this breaks `withTestEnv` overrides. Not re-listed as a new finding here;
> it is already tracked. Recommendation: schedule the migration as part of the N3 cache-key
> follow-up since both touch many slices.

### 6.4 P3 — polish

| #   | Finding                                                                                              | File:line                                                          |
| --- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| F6  | CORS allowlist is case-sensitive and trailing-slash-sensitive; no normalization in the Zod transform | `src/server.ts:133`; `src/config/zodEnv.ts:55–65`                  |
| F7  | Refresh cookie `sameSite: "strict"` + cross-origin dev setups may push forkers to weaken to `"none"` | `src/features/shared/auth/infrastructure/http/controller.ts:36–51` |
| —   | `x-request-id` header trusted unbounded                                                              | `src/shared/middleware/request-id.ts:16–18`                        |
| —   | `SENTRY_TRACES_SAMPLE_RATE` not clamped to `[0,1]` at schema layer                                   | `src/config/zodEnv.ts:241–246`                                     |

---

## 7. Risk-ranked recommendations

1. **Same-day patch PR — N1 + N2 + F1** (cumulative ≤2h).
   - Add `organizationId` to `vizKey` / `vizDashKey` (raw string + visible key).
   - Extend `VisualizationInvalidationPort` signature to take `organizationId`; thread it
     through every caller.
   - Add `superRefine` block to `DISABLE_RATE_LIMITING` that refuses `true` when
     `NODE_ENV=production`.
   - Add tests: (a) cross-org cache hit returns `null`; (b) `DISABLE_RATE_LIMITING=true` +
     `NODE_ENV=production` causes `loadEnvOrExit()` to throw.

2. **Within-week** (cumulative ≤2.5d).
   - N3 cache-key port refactor (require `organizationId` in `buildCursorCacheKey`; add
     arch-test rule).
   - N4 request-id propagation through RabbitMQ (publisher + consumer).
   - N5 OpenAPI: gate dummy routes by `NODE_ENV`; register their schemas.
   - F2, F3 quick wins (regex widen + RabbitMQ default refusal).
   - F4 stats endpoint range cap + SQL aggregation.
   - Close C5/C6 from the existing caveat list (Sentry `beforeSend` + redaction pattern
     widen) since they're 1h each.

3. **Within-month** (≤1d each).
   - C1/C3/C4 — finally close the open caveats from 2026-05-24.
   - N7 OpenTelemetry auto-instrumentation behind `OTEL_*` env.
   - N8 Sequelize pool env-tunables.
   - N9 split Redis client roles (cache vs invalidation vs rate-limit).
   - Resolve the ADR-003 disagreement (auth flat vs metric nested) — one ADR amendment +
     either migration.

4. **Deferred / open by design.**
   - N6 per-org queue routing (becomes urgent the moment a fair-use SLA is signed).
   - Phase 8 subscription/billing.
   - P2 items already on the deferred list: OAuth, APM (now N7), webhooks, feature flags.

---

## 8. Evidence appendix — files inspected

Pass 1 architecture (sampled): `scripts/bootstrap-fork.sh`; `src/config/app-name.ts`;
`src/shared/middleware/error.ts`; `__tests__/unit/architecture.test.ts`;
`src/features/public/metric/domain/entities/Metric.ts`;
`src/features/public/metric-settings/domain/entities/MetricSettings.ts`;
`src/features/shared/auth/infrastructure/persistence/` (full listing);
`src/features/public/metric/infrastructure/persistence/{mappers,models,repositories}/`;
`src/features/public/metric-log/infrastructure/persistence/...` (full repo audit);
`MetricRepoSequelize.ts`, `MetricLogRepoSequelize.ts`, `MetricCategoryRepoSequelize.ts`,
`MetricSettingsRepositorySequelize.ts`, `MembershipRepositorySequelize.ts`,
`MetricLogQueryRepoSequelize.ts`.

Pass 1 security: `src/server.ts`; `src/config/envManager.ts`; `src/config/zodEnv.ts`;
`src/config/sensitive-keys.ts`; `src/utils/logger.ts`;
`src/features/shared/auth/application/use-cases/RotateRefreshToken.ts`;
`src/features/shared/auth/infrastructure/persistence/RefreshTokenRepositorySequelize.ts`;
`src/features/shared/auth/infrastructure/providers/JwtTokenProvider.ts`;
`src/shared/middleware/rate-limiter.ts`; `src/shared/middleware/request-id.ts`;
`src/features/shared/auth/infrastructure/http/authMiddleware.ts`;
`src/features/shared/auth/application/use-cases/SwitchOrganization.ts`.

Pass 2 architecture: `VisualizationCacheRedis.ts` (verbatim-cited above);
`VisualizationInvalidationAdapter.ts`; `MetricCacheRedis.ts`; `MetricLogCacheRedis.ts`;
`src/shared/cache/keys.ts`; `src/shared/infrastructure/queue/topology.ts`,
`RabbitMQPublisher.ts`, `RabbitMQConsumer.ts`; `src/worker.ts`;
`GenerateDummyMetricLogsHandler.ts`; `docs/openapi/lakira-backend-openapi.json`
(routes section); `src/config/db.ts`; `src/utils/redis-client.ts`;
`MetricLogStatsRepoSequelize.ts`; `MetricLogQueryRepoSequelize.ts`;
all 8 public repos + queries — `:public-method-by-method`.

Pass 2 security: `src/config/zodEnv.ts` (full); `src/config/sensitive-keys.ts`;
`src/features/public/metric-log/infrastructure/http/schema.zod.ts`;
`src/features/public/metric-log/infrastructure/persistence/repositories/MetricLogStatsRepoSequelize.ts`;
`src/features/shared/auth/infrastructure/http/controller.ts`;
`src/server.ts` (CORS block); `package.json` (`@sentry/node`, no `@opentelemetry/*`).

Main thread direct re-verification of P0/HIGH findings: `VisualizationCacheRedis.ts`,
`VisualizationInvalidationAdapter.ts`, `zodEnv.ts:180–230` — see file:line citations above.

---

## 9. Out of scope for this run

- Closing C1–C6. They are tracked separately and remain ≤1-day each.
- Fixing N1+N2+F1. The patch is recommended but per `.claude/rules/workflow.md` § Commit &
  PR Ownership, the user runs all write-side git ops. A ready-to-use PR message for _this
  audit's docs_ is included in the closing report; the _source patches_ should be a
  separate PR off `dev`.
- Phase 8 (subscription/billing).
