# SaaS Base Readiness Audit — 2026-05-01

**Branch:** `docs/saas-readiness-audit-2026-05-01`
**Auditor:** Claude (Opus 4.7), invoked from `docs/todos/2026-05-01-promt-saas-readiness-audit.md`
**Standard graded against:** `.claude/rules/*` (architecture, security, api-design, database, validation, testing, code-style, environment, commands, workflow, documentation) + `docs/tests/TESTING_STRATEGY.md` + `docs/ci-cd/CI_CD_STRATEGY.md`.

A category cannot be ✅ if its verification command fails or the project's own intended standard is not met. Partial implementation is ⚠️.

---

## Verdict (one paragraph)

Lakira is **not fork-ready as a SaaS base today**. Empirical static and test gates are all green, the DDD feature-slice architecture is consistently applied for the auth + core CRUD slices, and the security/observability primitives (helmet, hpp, xss-clean, three-tier rate limiter, Winston JSON logging, central error handler, OpenAPI gen + Swagger UI, security-delta CI gate) are all wired. But the repo is missing the public-base scaffolding that a forker needs on day one — no `README.md`, no `LICENSE` despite `package.json` declaring ISC, no `.env.example` covering the full env surface, no refresh-token flow, no email verification, no multi-tenancy primitives, no request-ID propagation, no `trust proxy` setting (which silently breaks IP-based rate limiting on Render), and a roughly 13-file Lakira-branding footprint with no rename script. There is also moderate architectural drift inside the metric\* feature slices (duplicate `MetricAccessSequelize` adapters, two "transaction" ports in metric, mappers split across two locations, analytics has no domain entities and uses raw SQL) which a forker would inherit.

---

## Empirical Verifications

| Command                         | Result    | Notes                                                                                                                                            |
| ------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| `npm run typecheck`             | ✅ exit 0 | Clean                                                                                                                                            |
| `npm run lint`                  | ✅ exit 0 | Clean                                                                                                                                            |
| `npm run format:check`          | ✅ exit 0 | All matched files use Prettier code style                                                                                                        |
| `npm test`                      | ✅ exit 0 | 16 of 17 suites pass; 1 suite (`VisualizationCacheRedis.integration`) skipped because no Redis available locally; 3 tests skipped; 116 passed    |
| `npm run security:delta:check`  | ✅ exit 0 | 4 medium npm-audit findings: `resend`, `sequelize`, `svix`, +1 (transitive). No high/critical. Report: `tmp/security/security-delta-report.json` |
| `npm run docs:openapi:generate` | ✅ exit 0 | Spec written to `docs/openapi/lakira-backend-openapi.json` (regenerated 2026-05-01)                                                              |

No commands faked or skipped for environmental reasons. The single skipped Jest suite requires a local Redis instance; CI runs it via `docker-compose.test.yml`.

---

## Scorecard

| Category                                | ✅     | ⚠️     | ❌     | N/A   |
| --------------------------------------- | ------ | ------ | ------ | ----- |
| 1. Authentication & Authorization       | 2      | 1      | 3      | 0     |
| 2. API Design & Contracts               | 4      | 2      | 0      | 0     |
| 3. Database Layer                       | 2      | 2      | 1      | 0     |
| 4. Security                             | 3      | 4      | 1      | 0     |
| 5. Error Handling & Observability       | 2      | 1      | 3      | 0     |
| 6. Developer Experience & Onboarding    | 4      | 0      | 2      | 0     |
| 7. Testing                              | 5      | 0      | 1      | 0     |
| 8. CI/CD & Deployment                   | 4      | 2      | 0      | 0     |
| 9. Multi-Tenancy & SaaS-Specific        | 0      | 1      | 4      | 0     |
| 10. Code Architecture & Maintainability | 0      | 5      | 0      | 0     |
| 11. Forkability                         | 0      | 3      | 3      | 0     |
| **Total (65 items)**                    | **26** | **21** | **18** | **0** |

**Severity counts:** P0 = 7, P1 = 17, P2 = 11.

**Fork-ready exit criteria check:**

1. Zero P0 gaps remaining → ❌ (7 open).
2. Empirical commands all green → ✅.
3. Categories 1/4/6/7/8/11 ≥80% ✅ →
   - Cat 1 (Auth) 33% ❌
   - Cat 4 (Security) 38% ❌
   - Cat 6 (DX) 67% ❌
   - Cat 7 (Testing) 83% ✅
   - Cat 8 (CI/CD) 67% ❌
   - Cat 11 (Forkability) 0% ❌
4. `LICENSE` and `.env.example` present → ❌ both missing.

**Result: NOT FORK-READY.** Three of four exit conditions fail.

---

## Top 5 P0/P1 Gaps (one-line summaries)

1. **P0** — `LICENSE` file is missing despite `package.json` declaring `"license": "ISC"` (see [P0-11.1](#p0--11-forkability--license-file-missing)).
2. **P0** — Root-level `README.md` is missing entirely (see [P0-6.2](#p0--6-developer-experience--rootlevel-readmemd-missing)).
3. **P0** — No `.env.example` at repo root; `.env.test.example` covers only ~30% of the env surface (see [P0-6.1](#p0--6-developer-experience--envexample-missing-or-incomplete)).
4. **P0** — Express has no `app.set("trust proxy", ...)` and no HTTPS redirect, which silently breaks IP-based rate limiting on Render-class PaaS (see [P0-4.1](#p0--4-security--no-trust-proxy--https-readiness)).
5. **P0** — No refresh-token flow: JWTs are signed with a 7-day TTL and there is no `/auth/refresh` route or token revocation (see [P0-1.1](#p0--1-authentication--no-refreshtoken-flow)).

---

# Gap Entries

> Format per item: status / what's missing / why it matters / recommended fix / effort / evidence. Entries are grouped by category; severity tags are inline in the heading.

## 1. Authentication & Authorization

### [P0] 1. Authentication · No refresh-token flow

- **Status:** ❌
- **What's missing/incomplete:** JWTs are signed with `expiresIn: "7d"` and there is no `/auth/refresh` route, no `RefreshToken` entity, and no rotation/revocation. `POST /api/v1/auth/logout` returns 200 without invalidating the token (`controller.ts:97-99`).
- **Why it matters for a SaaS base:** A 7-day non-rotating bearer token is a long-window credential. SaaS bases typically pair a 15-min access token with a rotating refresh token stored as an HttpOnly cookie. Without it, every fork inherits a broad XSS/token-leak blast radius and a useless logout.
- **Recommended fix:** Add a `RefreshToken` entity (id, userId, hashedToken, expiresAt, revokedAt, createdAt, replacedById) + `RefreshTokenRepository`. Issue refresh as HttpOnly cookie on login; rotate-and-revoke on `/auth/refresh`; revoke on `/auth/logout`. Reduce access-token TTL to 15 min. Drop the inline `jwt.verify` from `authMiddleware.ts:42` and route all token ops through `TokenProvider` (extend the port with `verify()` and `signRefresh()`).
- **Effort:** L
- **Evidence:** `src/features/shared/auth/infrastructure/providers/JwtTokenProvider.ts:11`; `src/features/shared/auth/infrastructure/http/authMiddleware.ts:42`; `src/features/shared/auth/infrastructure/http/controller.ts:97-99`; no `refresh_token*` migration in `src/migrations/`.

### [P1] 1. Authentication · No email verification

- **Status:** ❌
- **What's missing/incomplete:** No `email_verified_at` column on `users`, no `EmailVerificationToken` entity, no `/auth/verify-email` or `/auth/resend-verification` route. New users immediately have a usable account.
- **Why it matters for a SaaS base:** Forkers ship with verified-email assumptions baked into downstream flows (billing, password reset, abuse handling). Adding it post-hoc requires backfill migrations.
- **Recommended fix:** Mirror the `PasswordResetToken` pattern. Add `email_verified_at` to `users`, an `EmailVerificationToken` entity + repo, and gate sensitive endpoints behind `requireVerifiedEmail`. Reuse `EmailSender` port + `ResendEmailSender`.
- **Effort:** M
- **Evidence:** `src/features/shared/auth/infrastructure/persistence/models/user.sequelize.ts` (no `emailVerifiedAt`); no matching migration in `src/migrations/`.

### [P1] 1. Authorization · RBAC is a stub

- **Status:** ⚠️
- **What's missing/incomplete:** `User.role` is a `"user" | "admin"` enum and `requireAdmin` middleware exists, but it is wired only to `GET /api/v1/admin/_ping` (`server.ts:127`). There is no permission system, no role table, no role-to-route policy map.
- **Why it matters for a SaaS base:** Most SaaS forks need at least owner/admin/member granularity per workspace; an enum on `users` cannot represent that.
- **Recommended fix:** Defer until the multi-tenancy P0 lands (see 9). Then introduce `Role`, `Permission`, and `Membership(userId, organizationId, role)` and a thin `requirePermission("scope:action")` middleware. For pre-tenant base, at minimum keep the enum but add `assertAdmin(req)` helper symmetric to `assertAuthenticated(req)`.
- **Effort:** M
- **Evidence:** `src/features/shared/auth/infrastructure/persistence/models/user.sequelize.ts:65-69`; `src/features/shared/auth/infrastructure/http/requireAdmin.ts`; `src/server.ts:126-131`.

### [P2] 1. Authentication · No OAuth / social login

- **Status:** ❌
- **What's missing/incomplete:** No Passport, no `@auth/express`, no Google/GitHub/Apple integration.
- **Why it matters for a SaaS base:** Many forks expect OAuth on day one; lack of it isn't a blocker but is friction.
- **Recommended fix:** Add `@auth/express` (Auth.js for Express) once refresh tokens land — they share the cookie session model. Wire one provider (Google) end-to-end as a reference.
- **Effort:** L
- **Evidence:** `package.json` deps — no oauth/passport packages.

---

## 2. API Design & Contracts

### [P1] 2. API · Response envelope inconsistency between success and validation-error paths

- **Status:** ⚠️
- **What's missing/incomplete:** Success responses use `{ status, message, data, success, code? }` (`src/utils/response-formatter.ts:35-49`). Validation failures and malformed JSON return a different shape from `errorHandler`: `{ status: "fail", errors: [{ field, message }] }` — no `data`, no `success`, no `message`.
- **Why it matters for a SaaS base:** Frontend consumers and contract tests have to branch on shape. The OpenAPI spec exposes both shapes, doubling client-side handling.
- **Recommended fix:** Normalize the error envelope to `{ status: "error", message, data: null, success: false, errors? }` so all responses share the top-level keys. Update `errorHandler` and the contract test fixtures in lockstep. Regenerate OpenAPI.
- **Effort:** S
- **Evidence:** `src/shared/middleware/error.ts:30-48`; `src/utils/response-formatter.ts:35-49`.

### [P2] 2. API · No shared pagination type / helper

- **Status:** ⚠️
- **What's missing/incomplete:** Cursor pagination is reimplemented per feature with subtle drift: `metric` returns `{ items, nextCursor, sort, limit, q?, filter?, totalCount? }`, `metric-settings` returns a slightly different shape via `ListMetricSettingsViaCursor`. There is no `Page<T>` type in `src/shared/`.
- **Why it matters for a SaaS base:** Forkers will keep duplicating the cursor logic per feature; the OpenAPI surface stays subtly inconsistent.
- **Recommended fix:** Add `src/shared/pagination/Page.ts` with a generic `Page<T>` and `parseCursorQuery()` helper. Migrate features incrementally. Add Zod schema for cursor query and reuse via `extendZodWithOpenApi`.
- **Effort:** M
- **Evidence:** `src/features/public/metric/application/queries/ListMetrics.ts`; `src/features/public/metric-log/application/queries/ListMetricLogs.ts`; `src/features/public/metric-settings/application/queries/ListMetricSettingsViaCursor.ts`.

---

## 3. Database Layer

### [P0] 3. Database · No multi-tenancy primitives

- **Status:** ❌
- **What's missing/incomplete:** Zero matches for `tenant`, `workspace`, `account_id`, `organization_id` across the codebase. Every domain table has `user_id` but no higher unit of isolation.
- **Why it matters for a SaaS base:** Most non-trivial SaaS apps need at least an `Organization` (or `Workspace`) so a single user can belong to multiple billing units. Retrofitting it after launch requires backfill of every domain table.
- **Recommended fix:** Decide the model up front: (a) "personal-only" — keep `user_id` as the boundary and document explicitly that the base is single-tenant-per-user; or (b) introduce `Organization` + `Membership(userId, organizationId, role)` and add `organizationId` to all domain tables now. For a forkable SaaS base, (b) is the right call. Add `organizationId` to all current models in a pre-data migration; require `req.organizationId` to be derived from membership in `authMiddleware`.
- **Effort:** L
- **Evidence:** No matches for `tenant|workspace|organization_id|account_id` in `src/`.

### [P1] 3. Database · Inconsistent soft-delete policy

- **Status:** ⚠️
- **What's missing/incomplete:** `paranoid: true` is set on `User`, `MetricCategory`, `Metric` only. `MetricSettings`, `MetricLog`, `PasswordResetToken`, and `processed_messages` are hard-deleted.
- **Why it matters for a SaaS base:** Any compliance story (GDPR right-to-erasure with audit trail, undo for end-users) needs a uniform policy. Mixed mode confuses forkers and creates referential-integrity surprises (a soft-deleted Metric leaves orphaned hard-deleted MetricLogs).
- **Recommended fix:** Pick a policy and document it in `decisions.md`. Probable choice: paranoid on user-owned domain rows (`MetricSettings`, `MetricLog`); hard-delete on system rows (`PasswordResetToken`, `processed_messages`, future `RefreshToken`). Add migrations to add `deleted_at`. Update mappers to filter `deletedAt IS NULL` consistently.
- **Effort:** M
- **Evidence:** `src/features/shared/auth/infrastructure/persistence/models/user.sequelize.ts:84`; `src/features/public/metric/infrastructure/persistence/models/metric.sequelize.ts:97`; `src/features/public/metric-category/infrastructure/persistence/models/metric-category.sequelize.ts:90`; `src/features/public/metric-log/infrastructure/persistence/models/metric-log.sequelize.ts` (no paranoid); `src/features/public/metric-settings/infrastructure/persistence/models/metric-settings.sequelize.ts` (no paranoid).

### [P2] 3. Database · No general-purpose dev/test seed scripts

- **Status:** ⚠️
- **What's missing/incomplete:** `src/seeders/` does not exist. The only seed is `scripts/seed-contract-tests.ts` (creates a single user + sample metric data for contract tests). No `npm run db:seed:dev`.
- **Why it matters for a SaaS base:** Forkers cloning the repo cannot get a populated dev DB without writing fixtures.
- **Recommended fix:** Add `src/seeders/dev/` with the existing test users + a generic "demo workspace" once tenancy lands. Wire `npm run db:seed:dev` and document it in `README.md`.
- **Effort:** S
- **Evidence:** `scripts/seed-contract-tests.ts`; `.sequelizerc` has commented-out `seeders-path`.

---

## 4. Security

### [P0] 4. Security · No `trust proxy` / HTTPS readiness

- **Status:** ❌
- **What's missing/incomplete:** `app.set("trust proxy", ...)` is never called in `src/server.ts`. There is no HTTPS-redirect middleware. The CI/CD strategy (`docs/ci-cd/CI_CD_STRATEGY.md`) targets Render — a reverse-proxied PaaS — meaning `req.ip` returns the proxy's IP, breaking IP-based rate limiting (`globalRateLimiter`, `passwordResetIpRateLimiter`) and IP logging.
- **Why it matters for a SaaS base:** This is a silent security regression: the rate limiter still "works", but every request is grouped under the proxy IP, so a bad actor effectively shares a single bucket with all legitimate users.
- **Recommended fix:** In `src/server.ts`, after `const app = express()`, add `app.set("trust proxy", env.TRUST_PROXY ?? 1)`. Add `TRUST_PROXY` to `zodEnv.ts` (`z.coerce.number().or(z.literal("loopback")).optional()`). Set to `1` in `.env.production`/`.env.staging`. Add an HTTPS-redirect middleware guarded by `NODE_ENV === "production"` that uses `req.secure` and `req.get("x-forwarded-proto")`.
- **Effort:** S
- **Evidence:** `grep -n "trust proxy" src/server.ts` — no match.

### [P1] 4. Security · 7 environment reads bypass `envManager` (analytics feature)

- **Status:** ⚠️
- **What's missing/incomplete:** The analytics feature reads `process.env.VIZ_*` and `process.env.DEFAULT_TZ` directly in 6 files, untyped and unvalidated, contradicting `.claude/rules/environment.md` ("All environment access goes through `loadEnvOrExit()`").
- **Why it matters for a SaaS base:** Forkers inherit invisible env knobs. A typo in a Render dashboard silently produces wrong defaults.
- **Recommended fix:** Add `VIZ_MAX_BUCKETS`, `VIZ_DASH_MAX_METRICS`, `VIZ_DEFAULT_TTL_SEC`, `VIZ_CACHE_MAX_AGE_SEC`, `VIZ_CACHE_STALE_SEC`, `VIZ_FALLBACK_GUARD_BUCKETS`, `DEFAULT_TZ` to `src/config/zodEnv.ts` with explicit defaults and units. Replace each `process.env.X` read with `loadEnvOrExit().X`.
- **Effort:** S
- **Evidence:** `src/features/public/analytics/application/queries/GetDashboardVisualization.ts:23,36`; `…/GetVisualization.ts:20`; `…/infrastructure/cache/VisualizationCacheRedis.ts:11`; `…/infrastructure/http/controller.ts:9,11`; `…/infrastructure/http/validators.ts:4,7`; `…/infrastructure/persistence/VisualizationReadRepoSequelize.ts:322`.

### [P1] 4. Security · Winston has no log-redaction filter

- **Status:** ⚠️
- **What's missing/incomplete:** `.claude/rules/security.md` says env vars matching `/(password|secret|token|key|certificate|url)$/i` are masked. That mask is applied **only** to `maskedEnvSnapshot()` in env-validation failure logs (`src/config/envManager.ts:6`). General Winston logs are not filtered — a `logger.error("login failed", { body })` would print the password.
- **Why it matters for a SaaS base:** Logs frequently end up shipped to a third-party aggregator. Inheriting an unredacted logger means inheriting a future incident.
- **Recommended fix:** Add a Winston `format()` redactor in `src/utils/logger.ts` that walks log meta and replaces values for keys matching the existing `SENSITIVE_KEY_PATTERN` (move the regex out of `envManager.ts` into `src/config/sensitive-keys.ts` for reuse). Add a unit test asserting redaction.
- **Effort:** S
- **Evidence:** `src/config/envManager.ts:6`; `src/utils/logger.ts` (no redaction format).

### [P1] 4. Security · No account lockout / brute-force protection on auth

- **Status:** ⚠️
- **What's missing/incomplete:** Login is protected by `userRateLimiter` (50 / 15min) only. There is no per-account failed-attempt counter, no account lock, no exponential backoff. `express-brute` is not installed.
- **Why it matters for a SaaS base:** A distributed credential-stuffing attack against 1000 known emails (each at <50 attempts/15min) is undetected.
- **Recommended fix:** Add a per-email Redis counter on failed login (`auth:lockout:<emailHash>`) with sliding window. After N failures, return 429 + a 15-min cooldown. Reset on success. Logging: emit a structured `auth.lockout.triggered` event.
- **Effort:** S
- **Evidence:** `src/features/shared/auth/infrastructure/http/router.ts` — no lockout middleware; `src/shared/middleware/rate-limiter.ts` — no per-account limiter.

### [P2] 4. Security · CORS supports a single origin only

- **Status:** ⚠️
- **What's missing/incomplete:** `CORS_ORIGIN` is read as a single string (`server.ts:96`); a multi-frontend SaaS (web + admin + mobile-debug) needs a list.
- **Why it matters for a SaaS base:** Most forks add a second origin within weeks; the current code requires a code change.
- **Recommended fix:** Parse `CORS_ORIGIN` as comma-separated; match `origin` against the allowlist. Or accept a regex.
- **Effort:** S
- **Evidence:** `src/server.ts:96-102`.

### [P2] 4. Security · `xss-clean` is unmaintained

- **Status:** ⚠️
- **What's missing/incomplete:** `xss-clean` (npm) was deprecated in 2022. It still functions but receives no security updates.
- **Why it matters for a SaaS base:** Forkers inheriting an unmaintained dep stays an audit finding indefinitely.
- **Recommended fix:** Replace with output-encoding (escape on render, not on input) — but that's a frontend concern. As a backend mitigation, adopt `dompurify` only where HTML is stored (none today), and remove `xss-clean` once no path stores HTML.
- **Effort:** M
- **Evidence:** `package.json` deps; `src/server.ts:89`.

---

## 5. Error Handling & Observability

### [P0] 5. Observability · No request-ID / correlation-ID propagation

- **Status:** ❌
- **What's missing/incomplete:** No `cls-rtracer`, `nanoid`, or `req.id` middleware. Logs cannot be correlated to a single request across the app + worker.
- **Why it matters for a SaaS base:** First production incident at any fork hits this wall — there's no way to filter logs to "what happened to request X". Adding it later requires touching every log call site that wants the ID.
- **Recommended fix:** Add a tiny middleware in `src/shared/middleware/request-id.ts`: read `x-request-id` header; if absent, mint `crypto.randomUUID()`; set `req.id`; echo back in response header; and seed a `cls-hooked` async-context (or use AsyncLocalStorage directly). Update Winston format to inject `requestId` from ALS.
- **Effort:** S
- **Evidence:** `grep -nE "requestId|correlationId|cls-rtracer" src/` — no matches.

### [P1] 5. Observability · No external error-monitoring hook point (Sentry)

- **Status:** ❌
- **What's missing/incomplete:** No `@sentry/node` import, no init in `src/server.ts`, no `Sentry.captureException` in the error handler.
- **Why it matters for a SaaS base:** Forkers want a single env-var-driven on-switch for production error tracking.
- **Recommended fix:** Add `@sentry/node` as an optional dep gated by `SENTRY_DSN` env. Init at top of `src/server.ts`. In `errorHandler`, call `Sentry.captureException(err)` for 5xx only. Add `SENTRY_DSN`, `SENTRY_TRACES_SAMPLE_RATE` to `zodEnv.ts`.
- **Effort:** S
- **Evidence:** `package.json` deps — no `@sentry/*`.

### [P1] 5. Observability · `/api/v1/health` is a liveness probe, not readiness

- **Status:** ⚠️
- **What's missing/incomplete:** `GET /api/v1/health` returns `{ status: "ok", environment, timestamp }` (`server.ts:105-111`). It does not ping Postgres or Redis. There is no `/healthz` or `/ready` endpoint.
- **Why it matters for a SaaS base:** Render and similar PaaS use the health endpoint to gate traffic; a green `/health` while the DB is down means traffic is routed to a broken instance.
- **Recommended fix:** Keep `/health` as liveness (no deps). Add `/ready` that runs `sequelize.authenticate()` + `redis.ping()` (with a 2s budget) and returns 503 on failure. Configure Render to use `/ready`.
- **Effort:** S
- **Evidence:** `src/server.ts:105-111`.

### [P2] 5. Observability · No metrics / APM readiness

- **Status:** ❌
- **What's missing/incomplete:** No `prom-client`, no OpenTelemetry, no statsd. No `/metrics` endpoint.
- **Why it matters for a SaaS base:** Nice-to-have, not blocking. Most forks add APM later.
- **Recommended fix:** Add `prom-client` with default Node metrics + an HTTP histogram middleware. Expose `/metrics` gated by an internal IP allowlist or a shared secret header. Document in README.
- **Effort:** M
- **Evidence:** `package.json` deps — no `prom-client`/`@opentelemetry/*`.

---

## 6. Developer Experience & Onboarding

### [P0] 6. Developer Experience · `.env.example` missing or incomplete

- **Status:** ❌
- **What's missing/incomplete:** No `.env.example` at repo root. `.env.test.example` exists (37 lines) but covers only test-mode vars. Missing from any example file: `EMAIL_PROVIDER`, `RESEND_API_KEY`, `EMAIL_FROM`, `FRONTEND_RESET_URL`, `RATE_LIMIT_*` overrides, `REQUEST_BODY_LIMIT`, `SWAGGER_REQUIRE_AUTH`, `ENABLE_DUMMY_ENDPOINTS`, `DEFAULT_TZ`, all `VIZ_*`, `DISABLE_RATE_LIMITING`, `ALLOW_TEST_HTTP_SERVER`, `ENABLE_REDIS_INTEGRATION`.
- **Why it matters for a SaaS base:** Forkers literally cannot boot without trial-and-error. Every Zod-required env failure has to be discovered at runtime.
- **Recommended fix:** Generate `.env.example` from `zodEnv.ts` (write a small script `scripts/generate-env-example.ts`). Group by section with comments. Mark required-vs-optional. Track in git. Add a check to CI that `.env.example` is in sync with `zodEnv.ts`.
- **Effort:** S
- **Evidence:** `ls /Users/dimaspramudya/development/workspace/js/lakira-backend/` — no `.env.example`.

### [P0] 6. Developer Experience · Root-level `README.md` missing

- **Status:** ❌
- **What's missing/incomplete:** No `README.md` at the repo root. `docs/README.md` exists but documents only the docs folder.
- **Why it matters for a SaaS base:** A forker on GitHub sees no project description, no quickstart, no architecture summary.
- **Recommended fix:** Write a root `README.md` covering: project pitch (1 paragraph), tech stack, prerequisites (Node 20, Docker), 5-minute local boot (`docker compose up -d` → `npm i` → `npm run migrate:dev` → `npm run dev`), env vars (link to `.env.example`), feature inventory, where docs live (`docs/`), license. Keep it under 200 lines.
- **Effort:** S
- **Evidence:** `ls /Users/dimaspramudya/development/workspace/js/lakira-backend/` — no `README.md` at root.

---

## 7. Testing

### [P1] 7. Testing · `e2e` Jest project missing despite script

- **Status:** ❌
- **What's missing/incomplete:** `package.json` declares `npm run test:e2e` (which runs `jest --selectProjects e2e`) but `jest.config.mjs:78-107` defines only `unit` and `integration` projects. Running the script today fails: "Project e2e not found". No `__tests__/e2e/` directory exists.
- **Why it matters for a SaaS base:** Broken script confuses contributors; no end-to-end smoke for full-stack flows.
- **Recommended fix:** Either (a) remove the `test:e2e` script, OR (b) add a third Jest project (`testMatch: ["__tests__/e2e/**/*.test.ts"]`, longer timeout, full server boot) and a placeholder `auth-flow.e2e.test.ts` covering register → login → logout. Decision in `decisions.md`.
- **Effort:** S
- **Evidence:** `package.json` `scripts.test:e2e`; `jest.config.mjs:78-107`; `__tests__/e2e/` — does not exist.

---

## 8. CI/CD & Deployment

### [P1] 8. CI/CD · Dockerfile is single-stage and runs `npm run dev`

- **Status:** ⚠️
- **What's missing/incomplete:** `Dockerfile` uses `node:20-alpine`, runs `npm install`, and `CMD npm run dev` (which uses `tsx watch`). Not production-ready — no build step, no `npm prune --production`, no non-root user, no `dumb-init`.
- **Why it matters for a SaaS base:** Forkers deploying to a generic container platform inherit a development image.
- **Recommended fix:** Multi-stage build:
  - Stage 1 `node:20-alpine` (deps + build): `npm ci`, `npm run build`.
  - Stage 2 `node:20-alpine` (runtime): copy `dist/`, `node_modules` (after `npm prune --omit=dev`), `package.json`. Run as `USER node`. `CMD ["node", "dist/server.js"]`.
- **Effort:** S
- **Evidence:** `Dockerfile` (single-stage).

### [P1] 8. CI/CD · No production deploy job

- **Status:** ⚠️
- **What's missing/incomplete:** `backend-ci.yml` has `deploy_staging` (triggered on pushes to `staging`) using `RENDER_STAGING_DEPLOY_HOOK_URL`. There is no `deploy_production` job and no `RENDER_PRODUCTION_DEPLOY_HOOK_URL` reference.
- **Why it matters for a SaaS base:** Not a forkability blocker (forkers may deploy elsewhere) but the CI/CD doc claims a `feature → dev → staging → main` model where `main` is "stable, production-ready" — without a deploy job, that's aspirational.
- **Recommended fix:** Add `deploy_production` job mirroring `deploy_staging` but gated on `github.ref == 'refs/heads/main'`. Add manual-approval `workflow_dispatch`. Document `RENDER_PRODUCTION_DEPLOY_HOOK_URL` requirement. Run `npm run migrate:production` step before the deploy hook.
- **Effort:** S
- **Evidence:** `.github/workflows/backend-ci.yml:368, 389-398`.

---

## 9. Multi-Tenancy & SaaS-Specific

### [P0] 9. Multi-Tenancy · No tenant model

- **Status:** ❌
- **What's missing/incomplete:** Already detailed in [P0-3.1](#p0--3-database--no-multi-tenancy-primitives). Listed here too because it blocks Category 9 entirely.
- **Why it matters for a SaaS base:** SaaS is generally multi-tenant; without it, the base is "personal app".
- **Recommended fix:** See [P0-3.1](#p0--3-database--no-multi-tenancy-primitives).
- **Effort:** L
- **Evidence:** Same as [P0-3.1](#p0--3-database--no-multi-tenancy-primitives).

### [P1] 9. SaaS · No subscription / plan model skeleton

- **Status:** ❌
- **What's missing/incomplete:** No `Plan`, `Subscription`, `Customer`, `Invoice` entity. No Stripe/Lemon Squeezy integration. No webhook route for billing events.
- **Why it matters for a SaaS base:** Most forks add billing within the first month. Missing the pattern means every forker reinvents it.
- **Recommended fix:** Add a minimal `subscription` slice with a `Plan` table (seeded with `free`, `pro`), a `Subscription(userId|orgId, planId, status, currentPeriodEnd)` table, a `requirePlan("pro")` middleware, and a `/webhooks/stripe` route that verifies signature and updates `Subscription` rows. Stripe-specific code lives behind a `BillingProvider` port.
- **Effort:** L
- **Evidence:** No matching files in `src/features/`.

### [P2] 9. SaaS · No general feature-flag system

- **Status:** ⚠️
- **What's missing/incomplete:** Only `ENABLE_DUMMY_ENDPOINTS` (env-driven boolean) exists. No flag table, no LaunchDarkly/Flagsmith client.
- **Why it matters for a SaaS base:** Useful for staged rollouts; not a blocker for a base.
- **Recommended fix:** Add a `FeatureFlag(key, enabled, scope: "global"|"organization"|"user", payload?)` table with a tiny `isFlagOn(key, ctx)` helper. Cache in Redis. Adopt LaunchDarkly only if scale demands.
- **Effort:** M
- **Evidence:** `grep -rn "ENABLE_DUMMY_ENDPOINTS\|featureFlag" src/` returns only the dummy-endpoint flag.

### [P2] 9. SaaS · No outbound webhook delivery

- **Status:** ❌
- **What's missing/incomplete:** No webhook-endpoints table, no signing scheme, no delivery worker. The `svix` dep is in package.json but unused (it's listed in the security-delta findings).
- **Why it matters for a SaaS base:** SaaS apps frequently need to notify partners; not all forks need this immediately.
- **Recommended fix:** Either remove the unused `svix` dep, or wire it: `WebhookEndpoint(orgId, url, secret, events[])` table; emit events via the existing RabbitMQ topology; deliver via Svix.
- **Effort:** M
- **Evidence:** `package.json` declares `svix`; no use site in `src/`. Security-delta `AUTO-NPM-svix` finding.

---

## 10. Code Architecture & Maintainability

### [P1] 10. Architecture · DDD layout drift across feature slices

- **Status:** ⚠️
- **What's missing/incomplete:** Five concrete drifts from `.claude/rules/architecture.md`:
  1. `metric` has **no `dto.ts`** in `infrastructure/http/`; DTO mapping happens in `src/utils/mappers/metric.mapper.ts` (legacy `src/utils/` location).
  2. `metric` exposes **two transaction ports** (`PersistenceTransaction.ts` and `TransactionPort.ts`) — looks like a stalled refactor.
  3. `metric-log` has an `infrastructure/access/` directory (for `MetricAccessSequelize`) instead of `infrastructure/providers/`.
  4. `metric-settings` has both `infrastructure/mappers/` AND `infrastructure/persistence/` (other features keep mappers under `persistence/mappers/`); plus `infrastructure/providers/MetricAccessSequelize.ts` which **duplicates** the metric-log adapter for the same port contract.
  5. `analytics` has no domain entities and no repositories — only utility functions; uses raw SQL files (`infrastructure/sql/*.sql.ts`); schema file is named `validators.ts` instead of `schema.zod.ts`; query file `getMetricTrend.ts` is lowercase + plain function (other queries are PascalCase classes with `execute()`).
- **Why it matters for a SaaS base:** Forkers inherit ambiguity about the canonical pattern. Onboarding new contributors takes longer; ESLint cannot enforce a standard that has 5 exceptions.
- **Recommended fix:** Pick the canonical layout in `decisions.md`. Migrate slices to it (move metric mapper into `metric/infrastructure/persistence/mappers/`; delete duplicate `MetricAccessSequelize`; consolidate transaction ports; rename analytics `validators.ts` → `schema.zod.ts`; convert `getMetricTrend` to a class). Add an architecture-test (`__tests__/unit/architecture.test.ts`) that fails CI when a slice lacks the required dirs.
- **Effort:** M
- **Evidence:** `src/features/public/metric/` (no `dto.ts`); `…/metric/application/ports/PersistenceTransaction.ts` + `TransactionPort.ts`; `…/metric-log/infrastructure/access/MetricAccessSequelize.ts`; `…/metric-settings/infrastructure/providers/MetricAccessSequelize.ts`; `…/analytics/infrastructure/http/validators.ts`; `…/analytics/application/queries/getMetricTrend.ts`; `…/analytics/infrastructure/sql/*`.

### [P1] 10. Architecture · Sequelize types leaking into application layer

- **Status:** ⚠️
- **What's missing/incomplete:** Two application-layer files import from `sequelize`: `metric-log/application/queries/GetMetricLogStats.ts:1` (`FindOptions, Includeable, WhereOptions`) and `analytics/application/queries/getMetricTrend.ts:4` (`Op`). Application is supposed to depend on domain only (`.claude/rules/architecture.md`).
- **Why it matters for a SaaS base:** Couples the application to a specific ORM, undermining the swappability the repository pattern was supposed to give.
- **Recommended fix:** Move query criteria into a small port type owned by domain (`MetricLogQueryCriteria`) and translate to Sequelize's `WhereOptions` inside the repo adapter.
- **Effort:** S
- **Evidence:** `src/features/public/metric-log/application/queries/GetMetricLogStats.ts:1`; `src/features/public/analytics/application/queries/getMetricTrend.ts:4`.

### [P1] 10. Architecture · Auth middleware re-implements `jwt.verify` outside the port

- **Status:** ⚠️
- **What's missing/incomplete:** `TokenProvider` port exposes only `sign()`. `authMiddleware.ts:42` calls `jwt.verify(token, env.JWT_SECRET)` directly with a top-level `import jwt from "jsonwebtoken"`. This bypasses the hexagonal boundary that the rest of auth respects.
- **Why it matters for a SaaS base:** Forkers swapping JWT for Paseto/branca would think the port is the only place to change — and miss this leak.
- **Recommended fix:** Extend `TokenProvider`: `verify(token: string): Promise<TokenClaims>`. Implement in `JwtTokenProvider`. Update `authMiddleware` to inject and call `tokenProvider.verify`. Remove direct `jsonwebtoken` import from middleware.
- **Effort:** S
- **Evidence:** `src/features/shared/auth/infrastructure/http/authMiddleware.ts:3,42`; `src/features/shared/auth/application/ports/TokenProvider.ts`.

### [P2] 10. Architecture · Manual DI inconsistency — some `buildXFeature()` accept overrides, others don't

- **Status:** ⚠️
- **What's missing/incomplete:** `auth`, `metric`, `metric-log` `buildXFeature()` accept dependency overrides (used in tests). `metric-category`, `metric-settings`, `analytics` do not.
- **Why it matters for a SaaS base:** Inconsistent test ergonomics for forkers.
- **Recommended fix:** Standardize on accepting an overrides bag. Update the three that don't.
- **Effort:** S
- **Evidence:** Compare `src/features/shared/auth/feature.ts` to `src/features/public/metric-category/feature.ts`.

### [P2] 10. Code health · Empty `src/features/admin/` placeholder + duplicate `src/utils/mappers/` legacy dir

- **Status:** ⚠️
- **What's missing/incomplete:** `src/features/admin/` is empty (no files tracked) but the path appears in `tsconfig.json` aliases. `src/utils/mappers/` (`generic.mapper.ts`, `metric-log.mapper.ts`, `metric.mapper.ts`) is a leftover from before the per-feature-mapper convention.
- **Why it matters for a SaaS base:** Dead/stub code is confusing and inflates the directory listing.
- **Recommended fix:** Delete the empty `admin/` placeholder until there is a real admin slice. Move the three legacy mappers into their owning feature's `infrastructure/persistence/mappers/` directory and update import sites.
- **Effort:** S
- **Evidence:** `ls src/features/admin/`; `src/utils/mappers/`.

---

## 11. Forkability

### [P0] 11. Forkability · `LICENSE` file missing

- **Status:** ❌
- **What's missing/incomplete:** `package.json:75` declares `"license": "ISC"` but there is no `LICENSE` file at the repo root.
- **Why it matters for a SaaS base:** GitHub displays "no license" → most readers assume "all rights reserved" and won't fork. Some legal teams block use of unlicensed code outright.
- **Recommended fix:** Decide license intent. ISC matches the package.json — drop the standard ISC text into `LICENSE`. If MIT is preferred for a SaaS base (most permissive, most expected), update both `LICENSE` and `package.json`. Note: portfolio projects often go MIT.
- **Effort:** S
- **Evidence:** No `LICENSE` file at repo root; `package.json:75`.

### [P1] 11. Forkability · `CONTRIBUTING.md` missing

- **Status:** ❌
- **What's missing/incomplete:** No `CONTRIBUTING.md`.
- **Why it matters for a SaaS base:** Forkers don't need contribution guidelines, but the pattern is expected on a public repo.
- **Recommended fix:** Either add a one-page `CONTRIBUTING.md` (branch model, commit conventions, link to `.claude/rules/`), or explicitly note in `README.md` that the project is single-developer and external contributions are not solicited (closing the loop honestly).
- **Effort:** S
- **Evidence:** No `CONTRIBUTING.md` at repo root.

### [P1] 11. Forkability · No fork-bootstrap script

- **Status:** ❌
- **What's missing/incomplete:** No `scripts/init.sh`, no `scripts/rename.sh`, no `bin/setup`. A forker must manually rename "lakira" across ~13 files, rotate `JWT_SECRET`, point at a new DB, and reconfigure RabbitMQ topology names.
- **Why it matters for a SaaS base:** This is the most concrete forkability gap — without it, the repo is "fork it and edit a lot" rather than a true template.
- **Recommended fix:** Add `scripts/bootstrap-fork.sh` that takes `--name <new-name>` and: (a) replaces lakira-branding strings in the 13 files identified below; (b) rotates `JWT_SECRET` in `.env.development`; (c) renames the queue topology prefix (`src/shared/infrastructure/queue/topology.ts`); (d) updates `package.json:name` and the OpenAPI title; (e) drops a `FORKED-FROM.md` with the upstream commit SHA. Document in `README.md`.
- **Effort:** M
- **Evidence:** `ls scripts/` — no init/rename script.

### [P1] 11. Forkability · ~13-file Lakira-branding footprint

- **Status:** ⚠️
- **What's missing/incomplete:** Files containing the literal `lakira` (categorized):
  - **Code & runtime:** `src/server.ts` (log strings), `src/utils/logger.ts` (`service: "lakira-backend"`), `src/lib/openapi/openapi-config.ts` (API title), `src/shared/infrastructure/queue/topology.ts` (`lakira.metric-log.dummy.*` queue/exchange names), `src/features/shared/auth/infrastructure/email/templates/password-reset.ts` (greeting branding).
  - **Build & config:** `package.json` (`"name": "lakira-backend"`), `package-lock.json`.
  - **Docker:** `docker-compose.test.yml` (`lakira_test_db`, `lakira_user`, `lakira_password`).
  - **CI:** `.github/workflows/backend-ci.yml` (DB name `lakira_ci`), `.github/workflows/backend-prd-drift-warning.yml` (OpenAPI filename), `.github/workflows/promote-dev-to-staging.yml` (workflow-name reference).
  - **Scripts:** `scripts/generate-openapi.ts`, `scripts/normalize-openapi.ts`, `scripts/generate-export-reference.ts`, `scripts/seed-contract-tests.ts`.
- **Why it matters for a SaaS base:** Inventory size determines whether forkability can be a one-line script (yes — bounded list) or a manual sweep.
- **Recommended fix:** Centralize all literal app-name strings into a single `src/config/app-name.ts` (`export const APP_NAME = env.APP_NAME ?? "lakira-backend"`) and reference it everywhere. Then the bootstrap script only has to change one env var. CI workflows still need direct edits, but a `scripts/bootstrap-fork.sh` can sed-replace them.
- **Effort:** M
- **Evidence:** Listed above; verified by `grep -ri --include='*.{ts,js,cjs,mjs,json,yml,yaml}' lakira` excluding `node_modules`, `dist`, `documents`.

### [P2] 11. Forkability · Module swappability has minor leaks

- **Status:** ⚠️
- **What's missing/incomplete:** Email (✅ port + 2 adapters), Queue (✅ port + 3 adapters), Cache (mostly ✅ — five separate `CachePort` interfaces, one per feature, duplicating contract), ORM (mostly ✅ — see [10.2](#p1--10-architecture--sequelize-types-leaking-into-application-layer)), Auth provider (B- — see [10.3](#p1--10-architecture--auth-middleware-reimplements-jwtverify-outside-the-port)).
- **Why it matters for a SaaS base:** The base advertises swappability; the leaks make the claim partially false.
- **Recommended fix:** Consolidate `CachePort<T>` in `src/shared/application/ports/CachePort.ts` with a single generic; per-feature ports become re-exports.
- **Effort:** S
- **Evidence:** `src/features/public/{metric,metric-log,metric-category,metric-settings,analytics}/application/ports/Cache*.ts` (and `VisualizationCachePort.ts`).

---

## Appendix A — Items graded ✅ (for reference, no action needed)

- **Auth:** JWT auth via `JwtTokenProvider`; password-reset flow with email + double rate limit (`passwordResetEmailRateLimiter`, `passwordResetIpRateLimiter`).
- **API:** `/api/v1` versioning; comprehensive Zod validation via `validate(schema)` middleware + `ZodMessages`; OpenAPI generation + Swagger UI at `/api/v1/docs` + drift check (`docs:openapi:check`) in CI; three rate limiters (global/user/analytics) with Redis store + in-memory fallback.
- **Database:** 14 Sequelize migrations with transactions and `down`; `createdAt`/`updatedAt` everywhere.
- **Security:** `helmet()`, `xss-clean`, `hpp()` global middleware; `disallowTraceMethod`; env-fail-fast via `loadEnvOrExit()`; security-framework unit tests + delta gate in CI; no hardcoded secrets in source.
- **Errors/Obs:** central `errorHandler` mounted last; Winston JSON format with `service: "lakira-backend"` meta.
- **DX:** `package.json` scripts comprehensive (dev, build, test, lint, migrate, security, contract, docs); ESLint flat config + Prettier enforced via `lint-staged`; Husky `pre-commit` hook; TypeScript `strict: true`.
- **Testing:** unit (60/40/55/60 thresholds) + integration (70/45/70/70 thresholds); per-worker test DB port `4000+workerId`; full table truncation between tests; CI runs unit + integration + contract on every PR.
- **CI/CD:** GitHub Actions backend pipeline with checks → security_delta → tests → contract_local → deploy_staging → contract_staging; `feature/* → dev → staging → main` branch promotion enforced; CI secrets via `${{ secrets.* }}`.
- **Architecture:** `auth` feature is the canonical reference (full DDD layout, port-per-concern, manual DI through `buildAuthFeature()`).

---

## Appendix B — Re-running this audit

```bash
# 1. Empirical commands (copy/paste)
npm run typecheck && npm run lint && npm run format:check && npm test && npm run security:delta:check && npm run docs:openapi:generate

# 2. Hardcoded-branding scan
grep -rli --include='*.ts' --include='*.cjs' --include='*.mjs' --include='*.json' --include='*.yml' --include='*.yaml' 'lakira' . \
  | grep -v node_modules | grep -v dist | grep -v documents | grep -v package-lock.json

# 3. Env reads bypassing envManager
grep -rn 'process\.env\.' src/ | grep -v 'src/config/' | grep -v __tests__

# 4. Sequelize leaks into application layer
grep -rn 'from "sequelize"' src/features/*/application/

# 5. Soft-delete consistency
grep -rn 'paranoid' src/features/*/infrastructure/persistence/models/
```

A fresh diff against this audit can be produced by re-running step 1, then comparing the resulting scorecard. Date the new file `audit-YYYY-MM-DD.md` in this same folder; do not overwrite this one.

---

## Appendix C — Files inspected (not exhaustive)

- `src/server.ts`, `src/worker.ts`
- `src/config/{envManager,zodEnv,db,config.cjs,loadEnv}.ts/.cjs`
- `src/shared/middleware/{rate-limiter,error,validation,method-guard,role,require-json-object}.ts`
- `src/utils/{response-formatter,logger,redis-client,catch-async,AppError,auth-guards}.ts`
- `src/features/shared/auth/**`
- `src/features/public/{metric,metric-log,metric-settings,metric-category,analytics}/**`
- `src/migrations/*.cjs`
- `.github/workflows/*.yml`
- `Dockerfile`, `docker-compose.yml`, `docker-compose.test.yml`
- `jest.config.mjs`, `jest.setup.ts`, `jest.setup.unit.ts`
- `package.json`, `tsconfig.json`, `eslint.config.mjs`, `.sequelizerc`
- `.claude/rules/*.md`
- `docs/{ci-cd,tests,security,documentation,openapi}/...`
