# SaaS Base Readiness Audit — 2026-05-20

**Branch:** `feat/phase-7-production-readiness`
**Auditor:** Claude (Opus 4.7), re-audit after Phases 0–7 of the remediation roadmap.
**Standard graded against:** `.claude/rules/*` (architecture, security, api-design, database, validation, testing, code-style, environment, commands, workflow, documentation) + `docs/tests/TESTING_STRATEGY.md` + `docs/ci-cd/CI_CD_STRATEGY.md` + ADR-001 in `decisions.md`.

A category cannot be ✅ if its verification command fails or the project's own intended standard is not met. Partial implementation is ⚠️. Status changes since `audit-2026-05-01.md` are annotated inline.

---

## Verdict (one paragraph)

Lakira is **functionally ready to ship as a SaaS base, but does not strictly satisfy ADR-001 fork-ready criteria due to one Category 4 sub-item.** Seven phases of remediation have closed every one of the 7 P0 gaps and 15 of 17 P1 gaps from `audit-2026-05-01.md`. The repo now has refresh tokens + token-family rotation, email verification, the canonical DDD layout enforced by an architecture-test, the `Organization` + `Membership` multi-tenancy primitive with `organizationId` on every domain table, request-ID propagation via AsyncLocalStorage with Winston correlation, a Sentry hook in the error handler, a Redis-backed login lockout, a multi-stage Dockerfile, a `deploy_production` CI job, an e2e Jest project, a `bootstrap-fork.sh` rename script, root `LICENSE` / `README.md` / `CONTRIBUTING.md` / `.env.example`, and `trust proxy` wired through `TRUST_PROXY` env. The remaining drag is a single P1 plus a handful of P2 ⚠️ items: six analytics files still read `process.env.VIZ_*` / `DEFAULT_TZ` directly (P1-4.2, never on any phase target), the success/error response envelopes still disagree (P1-2.1), soft-delete is still mixed across models (P1-3.2), CORS is still single-origin (P2-4.5), `xss-clean` is still resident (P2-4.6), `CachePort<T>` is consolidated in `src/shared/` but only `metric-category` re-exports it (P2-11.5), no OAuth / Prometheus / outbound webhooks / subscription model. Phase 8 (subscription) remains deferred by design. Strict ADR-001 reading: criterion #3 fails because Cat 4 sits at ~62% ✅ (three ⚠️ items unchanged). Pragmatic reading: the repo is shippable as a fork-base today; the gaps are tracked and well-scoped.

---

## Empirical Verifications

| Command                         | Result    | Notes                                                                                                         |
| ------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------- |
| `npm run typecheck`             | ✅ exit 0 | Clean                                                                                                         |
| `npm run lint`                  | ✅ exit 0 | Clean                                                                                                         |
| `npm run format:check`          | ✅ exit 0 | All matched files use Prettier code style                                                                     |
| `npm test`                      | ✅ exit 0 | 23 of 25 suites pass; 2 skipped (Redis-dependent integration); 163 passed / 5 skipped / 168 total             |
| `npm run security:delta:check`  | ✅ exit 0 | 1 medium npm-audit finding (down from 4). No high/critical. Report: `tmp/security/security-delta-report.json` |
| `npm run docs:openapi:generate` | ✅ exit 0 | Spec written to `docs/openapi/lakira-backend-openapi.json` (regenerated 2026-05-20)                           |

All six gates green. Skipped suites are the same Redis-dependent integration tests as in the original audit; CI runs them via `docker-compose.test.yml`.

---

## Scorecard

| Category                                | ✅     | ⚠️    | ❌    | N/A   | Δ vs 2026-05-01        |
| --------------------------------------- | ------ | ----- | ----- | ----- | ---------------------- |
| 1. Authentication & Authorization       | 5      | 0     | 1     | 0     | +3 ✅                  |
| 2. API Design & Contracts               | 4      | 2     | 0     | 0     | unchanged              |
| 3. Database Layer                       | 3      | 2     | 0     | 0     | +1 ✅, −1 ❌           |
| 4. Security                             | 5      | 3     | 0     | 0     | +2 ✅, −1 ❌           |
| 5. Error Handling & Observability       | 5      | 0     | 1     | 0     | +3 ✅, −2 ❌           |
| 6. Developer Experience & Onboarding    | 6      | 0     | 0     | 0     | +2 ✅, −2 ❌           |
| 7. Testing                              | 6      | 0     | 0     | 0     | +1 ✅, −1 ❌           |
| 8. CI/CD & Deployment                   | 6      | 0     | 0     | 0     | +2 ✅                  |
| 9. Multi-Tenancy & SaaS-Specific        | 2      | 1     | 2     | 0     | +2 ✅, −2 ❌           |
| 10. Code Architecture & Maintainability | 5      | 0     | 0     | 0     | +5 ✅                  |
| 11. Forkability                         | 5      | 1     | 0     | 0     | +5 ✅, −3 ❌           |
| **Total (65 items)**                    | **52** | **9** | **4** | **0** | +26 ✅, −12 ⚠️, −14 ❌ |

**Severity counts (open):** P0 = **0** (was 7), P1 = **4** (was 17), P2 = **9** (was 11).

**Fork-ready exit criteria check (ADR-001):**

1. Zero P0 gaps remaining → ✅ (all 7 closed).
2. Empirical commands all green → ✅.
3. Categories 1/4/6/7/8/11 ≥80% ✅ →
   - Cat 1 (Auth) 83% ✅
   - Cat 4 (Security) 62.5% ❌ (P1-4.2 env-reads + P2-4.5 CORS + P2-4.6 xss-clean still ⚠️)
   - Cat 6 (DX) 100% ✅
   - Cat 7 (Testing) 100% ✅
   - Cat 8 (CI/CD) 100% ✅
   - Cat 11 (Forkability) 83% ✅
4. `LICENSE` and `.env.example` present → ✅.

**Result: NOT FORK-READY by strict ADR-001 reading** — criterion #3 fails because Cat 4 is below the 80% bar. The blocking ⚠️ is P1-4.2 (six analytics files still read `process.env.VIZ_*` / `DEFAULT_TZ` directly); the other two below-bar items are both P2. **Pragmatic reading: shippable** — every P0 is closed, every critical-path category (1, 6, 7, 8, 11) clears the bar, and the remaining items are scoped feature additions or a single env-knob centralization sweep.

---

## Top 5 Remaining P0/P1 Gaps

No P0 gaps remain. The remaining P1 gaps:

1. **P1-4.2** — Six analytics files still read `process.env.VIZ_*` / `DEFAULT_TZ` directly, bypassing `envManager`. Never on a phase target; the only gap holding Cat 4 below the 80% bar.
2. **P1-2.1** — Success/error response envelopes still disagree. Error returns `{ status, errors[] }` or `{ status, message }` without `data` / `success` keys; success returns the full envelope. Frontend / contract tests still branch on shape.
3. **P1-3.2** — Soft-delete policy still mixed. `User`, `Metric`, `MetricCategory`, `Organization` are paranoid; `MetricSettings`, `MetricLog`, `Membership`, `OrganizationInvite`, `RefreshToken`, `EmailVerificationToken`, `PasswordResetToken` are hard-delete. No `decisions.md` entry codifying the rule.
4. **P1-9.2** — Subscription model: no `Plan` / `Subscription` table, no Stripe webhook. Phase 8 deferred by design (post-multi-tenancy); not a regression.

(That's only 4 — there is no fifth P1.)

---

# Gap Entries (re-graded)

> Format per item: status / what's missing / why it matters / evidence / **change since 2026-05-01**.

## 1. Authentication & Authorization

### [P0] 1. Authentication · No refresh-token flow

- **Status:** ✅ (was ❌)
- **Evidence:** `src/migrations/20260503000000-create-refresh-tokens.cjs`; `src/features/shared/auth/domain/entities/RefreshToken.ts`; `…/application/use-cases/IssueRefreshToken.ts`, `RotateRefreshToken.ts`, `RevokeRefreshTokenFamily.ts`; `…/infrastructure/http/router.ts:64` mounts `POST /refresh`. `TokenProvider.verify()` is now part of the port (`…/application/ports/TokenProvider.ts`); `authMiddleware.ts:50` uses `tokenProvider.verify(token)` — no direct `jsonwebtoken` import.
- **Change:** Phase 1 closed this. Refresh-token family with rotation + revocation, access token down to short TTL, port-level `verify()` so consumers can swap providers.

### [P1] 1. Authentication · No email verification

- **Status:** ✅ (was ❌)
- **Evidence:** `src/migrations/20260508000001-add-users-email-verified-at.cjs` + `20260508000002-create-email-verification-tokens.cjs`; `…/domain/entities/EmailVerificationToken.ts`; `…/application/use-cases/RequestEmailVerification.ts`, `VerifyEmail.ts`; router mounts `POST /verify-email` and `POST /resend-verification` (`router.ts:83`); `requireVerifiedEmail.ts` middleware gates sensitive endpoints.
- **Change:** Phase 3 closed this end-to-end. Reuses the `EmailSender` port from password-reset.

### [P1] 1. Authorization · RBAC is a stub

- **Status:** ✅ (was ⚠️)
- **Evidence:** `users.role` column dropped (`src/migrations/20260516000001-drop-users-role-column.cjs`). RBAC now via `Membership(userId, organizationId, role: "owner"|"admin"|"member")` (`…/domain/entities/Membership.ts:39`). `assertHasOrgRole.ts` + `requireOrgRole` middleware enforce per-org roles. `src/server.ts:196` applies `requireOrgRole("admin", "owner")` to the admin router.
- **Change:** Phase 4 replaced the user.role enum with org-scoped membership roles. The middleware is generic and reusable.

### [P2] 1. Authentication · No OAuth / social login

- **Status:** ❌ (unchanged)
- **Evidence:** No passport / @auth/express dep in `package.json`.
- **Change:** Not on any phase target. Still open by design.

---

## 2. API Design & Contracts

### [P1] 2. API · Response envelope inconsistency between success and validation-error paths

- **Status:** ⚠️ (unchanged)
- **Evidence:** `src/utils/response-formatter.ts:43-49` returns `{ status: "success", message, data, success: true, code? }`. `src/shared/middleware/error.ts:33-49` returns `{ status: "fail", errors: [{ field, message }] }` for validation/body-parse errors, and `{ status: "error", message }` for 500s — no `data`, no `success`, no `message` on the validation path.
- **Change:** Never targeted by a phase. Still inconsistent. Cheap S-effort fix; should fold into a future iteration plan as a standalone item.

### [P2] 2. API · No shared pagination type / helper

- **Status:** ⚠️ (unchanged)
- **Evidence:** No `src/shared/pagination/Page.ts`. Cursor logic still duplicated per feature.
- **Change:** Not targeted. Still drifts subtly across feature slices.

---

## 3. Database Layer

### [P0] 3. Database · No multi-tenancy primitives

- **Status:** ✅ (was ❌)
- **Evidence:** Migrations `20260510000001-create-organizations.cjs`, `20260510000002-create-memberships.cjs`, `20260510000003-create-organization-invites.cjs`, `20260510000004-backfill-organizations.cjs`, `20260510000005-add-organization-id-to-domain-tables.cjs`, `20260510000006-set-organization-id-not-null.cjs`. `organizationId` is `NOT NULL` on `metrics`, `metric_categories`, `metric_settings`, `metric_logs`, `refresh_tokens`. `__tests__/integration/features/metric/CrossOrgIsolation.integration.test.ts` proves row-level org boundary.
- **Change:** Phase 4 fully closed this. Active org derived from membership in `authMiddleware`; `SwitchOrganization` use case present.

### [P1] 3. Database · Inconsistent soft-delete policy

- **Status:** ⚠️ (unchanged)
- **Evidence:** `paranoid: true` on `User`, `Metric`, `MetricCategory`, `Organization`. Not on `MetricSettings`, `MetricLog`, `Membership`, `OrganizationInvite`, `RefreshToken`, `EmailVerificationToken`, `PasswordResetToken`, `processed_messages`. No ADR codifying the rule.
- **Change:** Not on a phase target. Policy is still de-facto rather than declared.

### [P2] 3. Database · No general-purpose dev/test seed scripts

- **Status:** ⚠️ (unchanged)
- **Evidence:** No `src/seeders/`. `.sequelizerc` still has `seeders-path` commented out. Only `scripts/seed-contract-tests.ts` exists.
- **Change:** Not targeted.

---

## 4. Security

### [P0] 4. Security · No `trust proxy` / HTTPS readiness

- **Status:** ✅ (was ❌)
- **Evidence:** `src/server.ts:95` — `app.set("trust proxy", env.TRUST_PROXY ?? 1)`. `src/config/zodEnv.ts:238` declares `TRUST_PROXY: z.coerce.number().optional()`. HTTPS-redirect middleware gated by `NODE_ENV === "production"` is in place.
- **Change:** Phase 0 closed this. Rate limiting on Render-class PaaS now sees real client IPs.

### [P1] 4. Security · 7 environment reads bypass `envManager` (analytics feature)

- **Status:** ⚠️ (unchanged)
- **Evidence:** Still 6 direct reads in analytics:
  - `…/analytics/application/queries/GetDashboardVisualization.ts:24,37` — `VIZ_MAX_BUCKETS`, `VIZ_DASH_MAX_METRICS`
  - `…/analytics/application/queries/GetVisualization.ts:21` — `VIZ_MAX_BUCKETS`
  - `…/analytics/infrastructure/cache/VisualizationCacheRedis.ts:11` — `VIZ_DEFAULT_TTL_SEC`
  - `…/analytics/infrastructure/http/schema.zod.ts:4,7` — `DEFAULT_TZ`, `VIZ_MAX_BUCKETS`
  - `…/analytics/infrastructure/http/controller.ts:9,11` — `VIZ_CACHE_MAX_AGE_SEC`, `VIZ_CACHE_STALE_SEC`
  - `…/analytics/infrastructure/persistence/VisualizationReadRepoSequelize.ts:334` — `VIZ_FALLBACK_GUARD_BUCKETS`
- **Change:** Never on a phase target. This is the single P1 that prevents Cat 4 from clearing the 80% bar — every other Cat 4 ⚠️ is a P2.

### [P1] 4. Security · Winston has no log-redaction filter

- **Status:** ✅ (was ⚠️)
- **Evidence:** `src/utils/logger.ts:26-37` defines a `redactSensitive` Winston `format()` that walks log meta and replaces values whose keys match the centralized `SENSITIVE_KEY_PATTERN` in `src/config/sensitive-keys.ts`. Wired at `logger.ts:61`.
- **Change:** Phase 2 closed this.

### [P1] 4. Security · No account lockout / brute-force protection on auth

- **Status:** ✅ (was ⚠️)
- **Evidence:** `src/features/shared/auth/infrastructure/http/loginLockout.ts` — Redis-backed per-email lockout. `LOCKOUT_THRESHOLD = 5`, `LOCKOUT_TTL_SECONDS = 15 * 60`, email hashed with SHA-256 before keying. Falls back to in-memory when Redis is unavailable.
- **Change:** Phase 7 closed this.

### [P2] 4. Security · CORS supports a single origin only

- **Status:** ⚠️ (unchanged)
- **Evidence:** `src/server.ts:135` — `origin: env.CORS_ORIGIN || "http://localhost:3000"`. `src/config/zodEnv.ts:53` — `CORS_ORIGIN: z.string().optional()`.
- **Change:** Not targeted.

### [P2] 4. Security · `xss-clean` is unmaintained

- **Status:** ⚠️ (unchanged)
- **Evidence:** `package.json:106` still lists `"xss-clean": "^0.1.4"`. `src/server.ts:5,126` still uses it.
- **Change:** Not targeted.

---

## 5. Error Handling & Observability

### [P0] 5. Observability · No request-ID / correlation-ID propagation

- **Status:** ✅ (was ❌)
- **Evidence:** `src/shared/middleware/request-id.ts` uses `AsyncLocalStorage<string>`; echoes `x-request-id` header back or mints `randomUUID()`. Mounted at `src/server.ts:109`. `src/utils/logger.ts:42-44` injects `requestId` into every log line from ALS. `__tests__/integration/middleware/request-id.test.ts` proves echo + UUID-v4 generation + per-request uniqueness.
- **Change:** Phase 2 closed this.

### [P1] 5. Observability · No external error-monitoring hook point (Sentry)

- **Status:** ✅ (was ❌)
- **Evidence:** `@sentry/node ^10.51.0` in `package.json`. `src/server.ts:45,57` initializes Sentry behind `SENTRY_DSN`. `src/shared/middleware/error.ts:68-70` calls `Sentry.captureException(err, { tags: { requestId: getRequestId() } })` for 5xx only.
- **Change:** Phase 2 closed this.

### [P1] 5. Observability · `/api/v1/health` is a liveness probe, not readiness

- **Status:** ✅ (was ⚠️)
- **Evidence:** `src/server.ts:151-170` exposes `/api/v1/ready` that pings `sequelize.authenticate()` and `redis.ping()` with a 2 s budget and returns 503 on failure. `__tests__/integration/health/ready.test.ts` covers all three branches (both ok / db fail / redis fail).
- **Change:** Phase 2 closed this.

### [P2] 5. Observability · No metrics / APM readiness

- **Status:** ❌ (unchanged)
- **Evidence:** No `prom-client` / `@opentelemetry/*` in deps.
- **Change:** Not targeted.

---

## 6. Developer Experience & Onboarding

### [P0] 6. Developer Experience · `.env.example` missing or incomplete

- **Status:** ✅ (was ❌)
- **Evidence:** `.env.example` exists at repo root.
- **Change:** Phase 0 closed this.

### [P0] 6. Developer Experience · Root-level `README.md` missing

- **Status:** ✅ (was ❌)
- **Evidence:** `README.md` exists at repo root.
- **Change:** Phase 0 closed this.

---

## 7. Testing

### [P1] 7. Testing · `e2e` Jest project missing despite script

- **Status:** ✅ (was ❌)
- **Evidence:** `jest.config.mjs:109-111` declares the third project with `displayName: "e2e"` and `testMatch: ["<rootDir>/__tests__/e2e/**/*.test.ts"]`. `__tests__/e2e/auth-flow.e2e.test.ts` exists. `jest.setup.e2e.ts` present.
- **Change:** Phase 7 closed this.

---

## 8. CI/CD & Deployment

### [P1] 8. CI/CD · Dockerfile is single-stage and runs `npm run dev`

- **Status:** ✅ (was ⚠️)
- **Evidence:** `Dockerfile` is now multi-stage: stage 1 `node:20-alpine AS build` runs `npm ci` + `npm run build`; stage 2 `node:20-alpine AS runtime` copies `dist/`, runs `npm ci --omit=dev`, switches to `USER node`, uses `dumb-init` as entrypoint, and runs `node dist/server.js`.
- **Change:** Phase 7 closed this. Production-grade image.

### [P1] 8. CI/CD · No production deploy job

- **Status:** ✅ (was ⚠️)
- **Evidence:** `.github/workflows/backend-ci.yml:434` defines `deploy_production`, gated after `deploy_staging` and contract checks.
- **Change:** Phase 7 closed this.

---

## 9. Multi-Tenancy & SaaS-Specific

### [P0] 9. Multi-Tenancy · No tenant model

- **Status:** ✅ (was ❌)
- **Evidence:** See P0-3.1 — same closure. `Organization`, `Membership`, `OrganizationInvite` domain entities + migrations + cross-org isolation integration tests.
- **Change:** Phase 4 closed this.

### [P1] 9. SaaS · No subscription / plan model skeleton

- **Status:** ❌ (unchanged)
- **Evidence:** No `subscription` / `plan` slice in `src/features/`. No Stripe webhook.
- **Change:** Phase 8 deferred by design (post-multi-tenancy). Not a regression; tracked in iteration plan.

### [P2] 9. SaaS · No general feature-flag system

- **Status:** ⚠️ (unchanged)
- **Evidence:** Only `ENABLE_DUMMY_ENDPOINTS` env boolean.
- **Change:** Not targeted.

### [P2] 9. SaaS · No outbound webhook delivery

- **Status:** ❌ (unchanged)
- **Evidence:** No `WebhookEndpoint` table. `svix` was removed from deps as part of dependency cleanup (`grep '"svix"' package.json` returns nothing) — so the audit's "remove `svix` or wire it" suggestion landed via the "remove" branch.
- **Change:** Not targeted. The previously-noted unused `svix` is gone.

---

## 10. Code Architecture & Maintainability

### [P1] 10. Architecture · DDD layout drift across feature slices

- **Status:** ✅ (was ⚠️)
- **Evidence:**
  - `metric/infrastructure/http/dto.ts` now exists (was missing).
  - Single `metric/application/ports/TransactionPort.ts` — `PersistenceTransaction.ts` removed.
  - `metric-log/infrastructure/access/` directory removed; `MetricAccessSequelize` consolidated to `metric/infrastructure/providers/MetricAccessSequelize.ts` (single canonical adapter).
  - `metric-settings/infrastructure/mappers/` removed; mappers consolidated under `metric-settings/infrastructure/persistence/mappers/`. Duplicate `metric-settings/infrastructure/providers/MetricAccessSequelize.ts` gone.
  - `analytics/infrastructure/http/schema.zod.ts` replaces `validators.ts`.
  - `__tests__/unit/architecture.test.ts` added — fails CI if a slice lacks `application/` / `infrastructure/` / `domain/`, if `src/utils/mappers/` reappears, if `src/features/admin/` reappears, or if a `feature.ts` lacks an overrides parameter.
- **Change:** Phase 5 closed this. Minor residual drift: auth + metric-category still keep mappers under `infrastructure/mappers/` rather than `infrastructure/persistence/mappers/`; the architecture-test does not yet enforce mapper location. Acceptable — both are valid placements per ADR-003's bracketed-optional layout.

### [P1] 10. Architecture · Sequelize types leaking into application layer

- **Status:** ✅ (was ⚠️)
- **Evidence:** `grep -rn 'from "sequelize"' src/features/*/application/ src/features/*/*/application/` returns no matches. `__tests__/unit/architecture.test.ts` enforces this going forward (one assertion per application-layer file).
- **Change:** Phase 5 closed this. `GetMetricLogStats.ts` and `getMetricTrend.ts` were refactored to use domain ports.

### [P1] 10. Architecture · Auth middleware re-implements `jwt.verify` outside the port

- **Status:** ✅ (was ⚠️)
- **Evidence:** `TokenProvider` port exposes `verify(token): Promise<TokenClaims>` (`src/features/shared/auth/application/ports/TokenProvider.ts:18-20`). `authMiddleware.ts:50` — `const claims = await tokenProvider.verify(token)`. No `import jwt from "jsonwebtoken"` in middleware.
- **Change:** Phase 1 closed this alongside refresh tokens.

### [P2] 10. Architecture · Manual DI inconsistency — some `buildXFeature()` accept overrides, others don't

- **Status:** ✅ (was ⚠️)
- **Evidence:** All six `feature.ts` files now accept an overrides bag: `auth`, `metric`, `metric-log`, `metric-settings`, `metric-category`, `analytics`. Architecture-test enforces it.
- **Change:** Phase 5 closed this.

### [P2] 10. Code health · Empty `src/features/admin/` placeholder + duplicate `src/utils/mappers/` legacy dir

- **Status:** ✅ (was ⚠️)
- **Evidence:** `ls src/features/admin/` exits 1 — directory removed. `ls src/utils/mappers` returns nothing. Architecture-test enforces both deletions.
- **Change:** Phase 5 closed this.

---

## 11. Forkability

### [P0] 11. Forkability · `LICENSE` file missing

- **Status:** ✅ (was ❌)
- **Evidence:** `LICENSE` exists at repo root.
- **Change:** Phase 0 closed this.

### [P1] 11. Forkability · `CONTRIBUTING.md` missing

- **Status:** ✅ (was ❌)
- **Evidence:** `CONTRIBUTING.md` exists at repo root.
- **Change:** Phase 6 closed this.

### [P1] 11. Forkability · No fork-bootstrap script

- **Status:** ✅ (was ❌)
- **Evidence:** `scripts/bootstrap-fork.sh` exists with `--name` arg, idempotent rename of `lakira-backend` → new-name across `package.json`, lock file, `docker-compose.test.yml`, CI workflows, scripts; rotates `JWT_SECRET`; sets `APP_NAME`; emits `FORKED-FROM.md`. Input validated against `^[a-z][a-z0-9-]*$` to prevent sed-delimiter injection.
- **Change:** Phase 6 closed this.

### [P1] 11. Forkability · ~13-file Lakira-branding footprint

- **Status:** ✅ (was ⚠️)
- **Evidence:** App-name is now centralized in `src/config/app-name.ts` (`APP_NAME`, `APP_SHORT_NAME`, `APP_DISPLAY_NAME`). Consumers: `src/utils/logger.ts:7` (`defaultMeta.service`), `src/lib/openapi/openapi-config.ts:6,17` (API title), `src/shared/infrastructure/queue/topology.ts:6-12` (queue / exchange prefix), `src/features/shared/auth/infrastructure/email/templates/password-reset.ts:1` (email greeting). The remaining `lakira` literals live in surfaces the bootstrap script handles: `package.json`, CI workflow YAML, `docker-compose.test.yml`, generator scripts.
- **Change:** Phase 6 closed this. A forker runs `./scripts/bootstrap-fork.sh --name my-app` and the runtime + CI surface flips in one shot.

### [P2] 11. Forkability · Module swappability has minor leaks

- **Status:** ⚠️ (unchanged in net effect)
- **Evidence:** `src/shared/application/ports/CachePort.ts` now defines the generic `CachePort<T>`. Only `metric-category` re-exports it (`src/features/public/metric-category/application/ports/CachePort.ts` extends `SharedCachePort<unknown>`). `metric`, `metric-log`, `analytics`, `metric-settings` still declare their own per-feature `CachePort` / `VisualizationCachePort` / `CacheInvalidationPort` interfaces.
- **Change:** Partial consolidation — the shared port exists but adoption is at 1 of 5 feature slices. Should be a follow-on S-effort sweep.

---

## Phase Performance Summary

| Phase | Targets                            | Closed?  | Notes                                                                                                                                               |
| ----- | ---------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0     | P0-4.1, P0-6.1, P0-6.2, P0-11.1    | ✅ all 4 | Trust proxy + HTTPS readiness, .env.example, README, LICENSE                                                                                        |
| 1     | P0-1.1, P1-10.3                    | ✅ all 2 | Refresh-token family + `TokenProvider.verify()` port; jwt.verify leak in middleware closed                                                          |
| 2     | P0-5.1, P1-5.2, P1-5.3, P1-4.3     | ✅ all 4 | Request-ID ALS + Sentry + /ready + Winston redactor (note: env-reads P1-4.2 was _not_ targeted)                                                     |
| 3     | P1-1.2                             | ✅       | Email verification end-to-end                                                                                                                       |
| 4     | P0-3.1, P0-9.1, P1-1.3             | ✅ all 3 | Organization + Membership + organizationId on all domain tables; users.role enum dropped                                                            |
| 5     | P1-10.1, P1-10.2                   | ✅ all 2 | Drift cleaned; architecture-test enforces; also incidentally closed P2-10.4 and P2-10.5                                                             |
| 6     | P1-11.2, P1-11.3, P1-11.4, P2-11.5 | ⚠️ 3/4   | CONTRIBUTING, bootstrap-fork.sh, APP_NAME centralization done. P2-11.5 CachePort consolidation only partial (1 of 5 slices re-exports shared port). |
| 7     | P1-4.4, P1-7.1, P1-8.3, P1-8.4     | ✅ all 4 | Login lockout + e2e Jest project + multi-stage Dockerfile + deploy_production job                                                                   |
| 8     | P1-9.2                             | 🅿️       | Deferred by design                                                                                                                                  |

**Phase 6 deviation:** P2-11.5 is reported as ⚠️ in this re-audit. The shared `CachePort<T>` exists and is wired for `metric-category`, but the four remaining feature slices still declare their own cache port interfaces. Either (a) close the consolidation in a small follow-up PR, or (b) accept the partial closure and amend the iteration plan to reflect that P2-11.5 lands "good enough" with a shared base port + one consumer.

---

## Appendix A — Items now graded ✅

These items closed since `audit-2026-05-01.md` (25 in total):

- **Cat 1:** Refresh tokens (P0-1.1), Email verification (P1-1.2), RBAC via Membership roles (P1-1.3).
- **Cat 3:** Multi-tenancy primitives (P0-3.1).
- **Cat 4:** Trust proxy + HTTPS (P0-4.1), Log redaction (P1-4.3), Brute-force lockout (P1-4.4).
- **Cat 5:** Request-ID propagation (P0-5.1), Sentry hook (P1-5.2), Readiness probe (P1-5.3).
- **Cat 6:** `.env.example` (P0-6.1), Root README.md (P0-6.2).
- **Cat 7:** e2e Jest project (P1-7.1).
- **Cat 8:** Multi-stage Dockerfile (P1-8.3), `deploy_production` CI job (P1-8.4).
- **Cat 9:** Tenant model (P0-9.1).
- **Cat 10:** DDD layout drift (P1-10.1), Sequelize leak in application (P1-10.2), jwt.verify outside port (P1-10.3), feature.ts override consistency (P2-10.4), empty admin + legacy mappers (P2-10.5).
- **Cat 11:** LICENSE (P0-11.1), CONTRIBUTING.md (P1-11.2), Fork-bootstrap script (P1-11.3), Lakira footprint via APP_NAME centralization (P1-11.4).

Plus the ~27 Appendix-A items already ✅ in `audit-2026-05-01.md` (JWT auth, password-reset, /api/v1 versioning, Zod validation, OpenAPI gen + Swagger + drift check, three-tier rate limiter, 14+ migrations with transactions, createdAt/updatedAt everywhere, helmet/xss-clean/hpp/disallowTraceMethod, env-fail-fast, security-framework tests + delta gate, no hardcoded secrets, central errorHandler, Winston JSON, npm scripts, ESLint flat + Prettier + lint-staged + husky, TypeScript strict, unit + integration coverage thresholds, per-worker test DB ports, CI on every PR, branch promotion enforced, GitHub Actions, secrets via `${{ secrets.* }}`, auth slice canonical DDD layout).

---

## Appendix B — Re-running this audit

```bash
# 1. Empirical commands (copy/paste)
npm run typecheck && npm run lint && npm run format:check && npm test && npm run security:delta:check && npm run docs:openapi:generate

# 2. Hardcoded-branding scan
grep -rli --include='*.ts' --include='*.json' --include='*.yml' --include='*.yaml' 'lakira' . \
  | grep -v node_modules | grep -v dist | grep -v documents | grep -v package-lock.json

# 3. Env reads bypassing envManager
grep -rn 'process\.env\.' src/ | grep -v 'src/config/' | grep -v __tests__

# 4. Sequelize leaks into application layer (uses find to avoid empty-glob shells)
find src/features -path '*/application/*' -name '*.ts' | xargs grep -l 'from "sequelize"' 2>/dev/null

# 5. Trust proxy
grep -n "trust proxy\|TRUST_PROXY" src/server.ts src/config/zodEnv.ts

# 6. Soft-delete consistency
find src/features -path '*/infrastructure/persistence/models/*.ts' | xargs grep -l 'paranoid'

# 7. Membership / multi-tenancy
ls src/features/shared/auth/domain/entities/ | grep -E 'Organization|Membership|Invite'
```

A fresh diff against this audit can be produced by re-running step 1, then comparing the resulting scorecard. Date the new file `audit-YYYY-MM-DD.md` in this same folder; do not overwrite this one.

---

## Appendix C — Files inspected

- `src/server.ts`, `src/utils/logger.ts`, `src/config/{envManager,zodEnv,app-name,sensitive-keys}.ts`
- `src/shared/middleware/{request-id,error,rate-limiter,method-guard,validation}.ts`
- `src/shared/application/ports/CachePort.ts`, `src/shared/infrastructure/queue/topology.ts`
- `src/features/shared/auth/**` (entities: Organization, Membership, OrganizationInvite, RefreshToken, EmailVerificationToken, PasswordResetToken, AuthUser; use-cases: 15 files; ports: TokenProvider, EmailSender, PasswordHasher, TokenHasher; http: 11 files inc. loginLockout, requireVerifiedEmail, assertHasOrgRole; mappers: 4)
- `src/features/public/{metric,metric-log,metric-settings,metric-category,analytics}/**` (feature.ts, dto.ts, schema.zod.ts, repos, mappers, ports)
- `src/migrations/*.cjs` (26 files)
- `.github/workflows/backend-ci.yml` (deploy_staging line 361, deploy_production line 434)
- `Dockerfile` (multi-stage), `docker-compose.{,test}.yml`, `jest.config.mjs`, `jest.setup.e2e.ts`
- `__tests__/unit/architecture.test.ts`, `__tests__/integration/{health/ready,middleware/request-id,features/metric/CrossOrgIsolation}.test.ts`, `__tests__/e2e/auth-flow.e2e.test.ts`
- `scripts/bootstrap-fork.sh`, `scripts/security/security-delta-check.mjs`
- `package.json` (deps + scripts), `tsconfig.json`, `.env.example`, `LICENSE`, `README.md`, `CONTRIBUTING.md`
