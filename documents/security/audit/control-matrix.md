# Control Matrix – Lakira Backend Security Audit

- **Generated:** Fri Nov 21 17:55:00 WIB 2025
- **Sources:** `documents/security-audit-log-baseline-simple.md`, `documents/security-audit-log-features-simple.md`, and `documents/threat-model.md`.

| # | Threat / Finding | Control Objective | Existing Control(s) | Gap / Status | Recommended Action |
| --- | --- | --- | --- | --- | --- |
| 1 | Privilege escalation via signup (T1) | Ensure only trusted admins can assign elevated roles | Signup schema/service now force `role = user` (`src/features/auth/infrastructure/http/schema.zod.ts`, `src/features/auth/application/use-cases/RegisterUser.ts`) | **Mitigated** | Monitor for future admin workflows before reintroducing role field |
| 2 | World-writable “public” metrics (T2) | Enforce per-tenant ownership on metric/log/settings CRUD | Ownership helpers now require matching `userId`; metrics default to private (`src/utils/db-helper.ts`, `src/constants/zod/zod-rules.ts`) | **Mitigated** | Add migrations/test coverage to prevent regressions |
| 3 | Database TLS trusts any certificate (T3) | Confidentiality/integrity of DB traffic | Sequelize configs now honor `DB_SSL_REJECT_UNAUTHORIZED` (default true) for staging/prod (`src/config/db.ts`, `src/config/config.cjs`) | **Mitigated** | Monitor certificates; override env only when using trusted proxies |
| 4 | Swagger docs exposed publicly (T4) | Limit attack surface & endpoint enumeration | Swagger UI now requires auth unless `SWAGGER_REQUIRE_AUTH=false` (`src/server.ts`) | **Mitigated** | Only disable guard for local diagnostics |
| 5 | Unlimited JSON payload size (T5) | Prevent request-body DoS | Express body parser limited via `REQUEST_BODY_LIMIT` env (default 1 MB) (`src/server.ts`, `src/config/zodEnv.ts`) | **Mitigated** | Tune limit per deployment; add compression if needed |
| 6 | Ineffective rate limiting when Redis disabled (T6) | Provide consistent throttling across envs | Redis connection required outside tests, fallbacks logged, and limiter caps configurable via env (`src/utils/redis-client.ts`, `src/shared/middleware/rate-limiter.ts`, `src/config/zodEnv.ts`) | **Mitigated** | Monitor Redis health and adjust limits as traffic evolves |
| 7 | Weak default DB credentials (T7) | Prevent trivial credential guessing | DB user/password/name now required env vars; parsing fails fast if missing (`src/config/zodEnv.ts`) | **Mitigated** | Keep secrets in deployment configs; add smoke tests for missing envs |
| 8 | Login lacks validation & throttling (T8) | Reduce brute-force and payload abuse | `/auth/login` now validated via `loginUserSchema` and guarded by `userRateLimiter` (`src/features/auth/infrastructure/http/router.ts`, `src/features/auth/infrastructure/http/schema.zod.ts`) | **Mitigated** | Consider CAPTCHA/2FA for additional protection |
| 9 | Dummy data endpoints exposed (T9) | Prevent abuse of testing utilities | Dummy routes only register when `ENABLE_DUMMY_ENDPOINTS=true` (defaults off outside dev) (`src/routes/metric.routes.ts`, `src/routes/metric-log.routes.ts`, `src/config/zodEnv.ts`) | **Mitigated** | Keep flag disabled in prod; remove entirely when fixtures no longer needed |
|10| Analytics visualization DoS (T10) | Protect heavy aggregation queries | Shared analytics rate limiter throttles dashboard/metric viz routes (`src/shared/middleware/rate-limiter.ts`, `src/features/analytics/infrastructure/http/visualization.router.ts`) | **Mitigated** | Monitor usage metrics and tune `RATE_LIMIT_ANALYTICS_MAX` as needed |
|11| Sensitive identifiers logged (T11) | Prevent leakage of user/metric IDs | Cache/routes/services now use Winston logger instead of `console.log` (e.g., `src/routes/metric-log.routes.ts`, `src/features/metric-log/infrastructure/http/controller.ts`, `src/features/metric-log/application/queries/listMetricLogs.ts`) | **Mitigated** | Keep logging at debug/info levels and avoid dumping raw payloads |
|12| Config/secret drift (T12) | Keep consistent hardening across tooling/runtime | Sequelize config now shells out via an inline `ts-node` loader snippet so migrations reuse the runtime Zod validation (`src/config/config.cjs`) | **Mitigated** | Ensure the CLI environment exposes the same env vars when running migrations |
|13| Redis failure exits process (Baseline §5.6) | Maintain availability despite cache issues | Redis client only exits when `REDIS_REQUIRED=true`; otherwise logs and continues (`src/utils/redis-client.ts`) | **Mitigated** | Monitor Redis health and alert when fallbacks engage |
|14| Rate limiter global settings (Baseline §5.4) | Ensure production-ready throttling | Global/user/analytics limiter caps are configurable via validated env vars (`src/config/zodEnv.ts`, `src/shared/middleware/rate-limiter.ts`) | **Mitigated** | Set per-environment values for `RATE_LIMIT_*` to suit traffic patterns |
|15| Security middleware baseline | Harden HTTP layer | Helmet, xss-clean, hpp, global limiter configured | **In Place** | Periodically review configs; add body-size limit and compression |
|16| Error handling hygiene | Avoid leaking stack traces in prod | Central `errorHandler` hides details when `NODE_ENV=production` | **In Place** | Ensure consistent use; add correlation IDs/log context |
